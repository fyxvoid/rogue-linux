$schema: http://json-schema.org/draft-07/schema#
type: object
required:
  - steps

properties:
  steps:
    type: array
    items:
      type: string
    description: >
      Ordered shell commands used to install artifacts.
      May assume build artifacts already exist.

additionalProperties: false
