[identity]
        name = "cycB"
        version = "1.0"
        category = "base"
        summary = "test"

        [identity.source]
        kind = "tarball"
        file = "cycB.tar"

        [identity.depends]
build = ["base/cycA"]

        [build]
        system = "make"
        steps = ["echo build cycB"]

        [installer]
        steps = ["echo install cycB"]