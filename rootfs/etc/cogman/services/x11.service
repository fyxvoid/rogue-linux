[service]
name          = x11
command       = /bin/su -l void -c 'startx -- :0 vt1 2>/tmp/xorg.log'
type          = process
restart       = on-failure
restart_delay = 3
depends       = rcs, mdev

[env]
PATH    = /usr/bin:/bin:/usr/sbin:/sbin
TERM    = xterm-256color
