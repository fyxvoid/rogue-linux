[service]
name          = tty6
command       = /sbin/getty -L tty6 0 linux
type          = process
restart       = always
restart_delay = 2
depends       = rcs

[env]
TERM = linux
HOME = /root
PATH = /usr/bin:/bin:/usr/sbin:/sbin
