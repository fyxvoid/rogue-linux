[service]
name          = tty1
command       = /sbin/getty -L -a void tty1 0 linux
type          = process
restart       = always
restart_delay = 2
depends       = rcs

[env]
TERM = linux
HOME = /home/void
PATH = /usr/bin:/bin:/usr/sbin:/sbin
