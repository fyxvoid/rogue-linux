[service]
name          = tty3
command       = /sbin/getty -L tty3 0 linux
type          = process
restart       = always
restart_delay = 2
depends       = rcs

[env]
TERM = linux
HOME = /root
PATH = /usr/bin:/bin:/usr/sbin:/sbin
