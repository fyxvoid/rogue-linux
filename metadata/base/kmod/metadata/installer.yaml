steps:
  - |
      echo "[COGMAN] Building kmod, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing kmod into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
