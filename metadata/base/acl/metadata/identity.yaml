name: acl
version: "2.3.2"
category: base

summary: >
  Access Control List utilities and library

source:
  kind: tarball
  file: acl-2.3.2.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/attr
