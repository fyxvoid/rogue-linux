steps:
  - |
      echo "[COGMAN] Building xz-utils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing xz-utils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
