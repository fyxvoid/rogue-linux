name: libelf
version: "0.191"
category: base

summary: >
  ELF object file access library from elfutils

source:
  kind: tarball
  file: libelf-0.191.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/compression/zlib
