name: linux-pam
version: "1.6.0"
category: base

summary: >
  Pluggable Authentication Modules framework

source:
  kind: tarball
  file: linux-pam-1.6.0.tar.xz

depends:
  build:
    - toolchain/gcc
