name: grep
version: "3.11"
category: base

summary: >
  GNU Grep pattern matching utility for searching text

source:
  kind: tarball
  file: grep-3.11.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/general/pcre2
