name: dosfstools
version: "4.2"
category: base

summary: >
  FAT filesystem utilities for creating and checking FAT volumes

source:
  kind: tarball
  file: dosfstools-4.2.tar.xz

depends:
  build:
    - toolchain/gcc
