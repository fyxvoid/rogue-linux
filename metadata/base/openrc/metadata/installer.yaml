steps:
  - |
      echo "[COGMAN] Building openrc, sir."
      cd build
      ninja

  - |
      echo "[COGMAN] Installing openrc into staging root, sir."
      cd build
      DESTDIR="$PKGROOT" ninja install
