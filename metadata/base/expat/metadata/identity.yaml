name: expat
version: "2.6.2"
category: base

summary: >
  XML parsing library for C with stream-oriented interface

source:
  kind: tarball
  file: expat-2.6.2.tar.xz

depends:
  build:
    - toolchain/gcc
