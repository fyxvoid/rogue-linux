steps:
  - |
      echo "[COGMAN] Building e2fsprogs, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing e2fsprogs into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
