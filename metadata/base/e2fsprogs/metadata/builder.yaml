steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting e2fsprogs source, sir."
      tar -xf tar/e2fsprogs-1.47.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring e2fsprogs, sir."
      cd build
      ../source/e2fsprogs-1.47.0/configure --prefix=/usr \
        --sysconfdir=/etc \
        --enable-elf-shlibs \
        --disable-fsck \
        --disable-uuidd

