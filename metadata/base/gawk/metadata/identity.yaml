name: gawk
version: "5.3.0"
category: base

summary: >
  GNU AWK pattern scanning and processing language

source:
  kind: tarball
  file: gawk-5.3.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
