steps:
  - |
      echo "[COGMAN] Building gawk, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gawk into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
