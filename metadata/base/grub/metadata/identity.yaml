name: grub
version: "2.12"
category: base

summary: >
  GRand Unified Bootloader for BIOS and UEFI systems

source:
  kind: tarball
  file: grub-2.12.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/flex
    - toolchain/bison
