steps:
  - |
      echo "[COGMAN] Building grub, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing grub into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
