steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting file source, sir."
      tar -xf tar/file-5.45.tar.xz -C source

  - |
      echo "[COGMAN] Configuring file, sir."
      cd build
      ../source/file-5.45/configure --prefix=/usr \
        

