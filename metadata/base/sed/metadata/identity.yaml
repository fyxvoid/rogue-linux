name: sed
version: "4.9"
category: base

summary: >
  GNU Stream Editor for filtering and transforming text

source:
  kind: tarball
  file: sed-4.9.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
