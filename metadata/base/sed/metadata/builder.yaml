steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting sed source, sir."
      tar -xf tar/sed-4.9.tar.xz -C source

  - |
      echo "[COGMAN] Configuring sed, sir."
      cd build
      ../source/sed-4.9/configure --prefix=/usr \
        

