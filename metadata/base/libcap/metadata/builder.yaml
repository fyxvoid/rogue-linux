steps:
  - rm -rf build source
  - mkdir -p build source

  - |
      echo "[COGMAN] Extracting libcap source, sir."
      tar -xf tar/libcap-2.69.tar.xz -C source

  - |
      echo "[COGMAN] Building libcap, sir."
      cd source/libcap-2.69

      make \
        prefix=/usr \
        lib=lib
