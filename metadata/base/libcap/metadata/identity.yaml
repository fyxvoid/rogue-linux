name: libcap
version: "2.69"
category: base

summary: >
  POSIX capabilities library for Linux

source:
  kind: tarball
  file: libcap-2.69.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
