steps:
  - |
      echo "[COGMAN] Building diffutils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing diffutils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
