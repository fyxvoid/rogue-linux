name: attr
version: "2.5.2"
category: base

summary: >
  Extended attributes filesystem utilities and library

source:
  kind: tarball
  file: attr-2.5.2.tar.xz

depends:
  build:
    - toolchain/gcc
