steps:
  - |
      echo "[COGMAN] Building attr, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing attr into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
