steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting gzip source, sir."
      tar -xf tar/gzip-1.13.tar.xz -C source

  - |
      echo "[COGMAN] Configuring gzip, sir."
      cd build
      ../source/gzip-1.13/configure --prefix=/usr \
        

