steps:
  - |
      echo "[COGMAN] Building gzip, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gzip into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
