steps:
  - |
      echo "[COGMAN] Building man-pages, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing man-pages into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
