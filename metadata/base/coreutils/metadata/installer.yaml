steps:
  - |
      echo "[COGMAN] Building coreutils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing coreutils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
