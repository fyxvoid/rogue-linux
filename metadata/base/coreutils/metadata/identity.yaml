name: coreutils
version: "9.5"
category: base

summary: >
  GNU Core Utilities providing essential file shell and text commands

source:
  kind: tarball
  file: coreutils-9.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
