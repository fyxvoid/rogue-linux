steps:
  - |
      echo "[COGMAN] Building pkg-config, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pkg-config into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
