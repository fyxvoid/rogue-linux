name: pkg-config
version: "0.29.2"
category: toolchain

summary: >
  Package configuration query tool for compiler and linker flags

source:
  kind: tarball
  file: pkg-config-0.29.2.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
