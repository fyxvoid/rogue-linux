steps:
  - |
      echo "[COGMAN] Building binutils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing binutils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
