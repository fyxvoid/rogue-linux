steps:
  - |
      echo "[COGMAN] Building mpfr, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mpfr into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
