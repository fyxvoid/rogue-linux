steps:
  - |
      echo "[COGMAN] Building autoconf, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing autoconf into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
