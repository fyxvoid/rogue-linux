steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bison source, sir."
      tar -xf tar/bison-3.8.2.tar.xz -C source

  - |
      echo "[COGMAN] Configuring bison, sir."
      cd build
      ../source/bison-3.8.2/configure --prefix=/usr \
        

