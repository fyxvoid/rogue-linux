name: perl
version: "5.38.2"
category: toolchain

summary: >
  Practical Extraction and Report Language interpreter

source:
  kind: tarball
  file: perl-5.38.2.tar.gz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
