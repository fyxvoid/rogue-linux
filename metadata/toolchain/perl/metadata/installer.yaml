steps:
  - |
      echo "[COGMAN] Building perl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing perl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
