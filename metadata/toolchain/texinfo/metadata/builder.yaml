steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting texinfo source, sir."
      tar -xf tar/texinfo-7.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring texinfo, sir."
      cd build
      ../source/texinfo-7.1/configure --prefix=/usr \
        

