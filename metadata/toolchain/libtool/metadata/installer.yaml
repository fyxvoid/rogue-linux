steps:
  - |
      echo "[COGMAN] Building libtool, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libtool into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
