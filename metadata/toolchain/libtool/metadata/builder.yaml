steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting libtool source, sir."
      tar -xf tar/libtool-2.4.7.tar.xz -C source

  - |
      echo "[COGMAN] Configuring libtool, sir."
      cd build
      ../source/libtool-2.4.7/configure --prefix=/usr \
        

