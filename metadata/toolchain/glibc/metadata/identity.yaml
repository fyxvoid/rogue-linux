name: glibc
version: "2.39"
category: toolchain

summary: >
  GNU C Library — provides the core system C runtime,
  binding userspace programs to the Linux kernel.

source:
  kind: tarball
  url: https://ftp.gnu.org/gnu/glibc/glibc-2.39.tar.xz
  file: glibc-2.39.tar.xz

depends:
  build:
    - toolchain/binutils
    - toolchain/gcc
    - toolchain/linux-headers
