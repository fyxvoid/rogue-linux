name: libstdc++
version: "14.1.0"
category: toolchain

summary: >
  GNU Standard C++ Library providing C++ standard library implementation

source:
  kind: tarball
  file: libstdc++-14.1.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
