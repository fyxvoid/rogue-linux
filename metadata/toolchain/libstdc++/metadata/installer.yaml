steps:
  - |
      echo "[COGMAN] Building libstdc++, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libstdc++ into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
