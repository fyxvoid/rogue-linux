steps:
  - |
      echo "[COGMAN] Building gettext, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gettext into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
