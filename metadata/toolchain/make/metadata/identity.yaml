name: make
version: "4.4.1"
category: toolchain

summary: >
  GNU Make build automation tool

source:
  kind: tarball
  file: make-4.4.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
