steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting make source, sir."
      tar -xf tar/make-4.4.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring make, sir."
      cd build
      ../source/make-4.4.1/configure --prefix=/usr \
        

