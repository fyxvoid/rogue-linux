steps:
  - |
      echo "[COGMAN] Building cmake, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cmake into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
