install:
  - ./configure
      --prefix=/tools
      --disable-nls
      --disable-multilib
      --enable-languages=c
      --without-headers
  - make
  - make install

uninstall:
  - make uninstall
