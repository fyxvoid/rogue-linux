steps:
  - |
      echo "[COGMAN] Building mpc, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mpc into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
