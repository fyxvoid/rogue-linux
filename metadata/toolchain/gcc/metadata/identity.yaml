name: gcc
version: "14.1.0"
category: toolchain

summary: >
  GNU Compiler Collection providing C and C++ compilers for the bootstrap system

source:
  kind: tarball
  file: gcc-14.1.0.tar.xz

depends:
  build:
    - toolchain/binutils
    - toolchain/glibc
    - toolchain/gmp
    - toolchain/mpfr
    - toolchain/mpc
