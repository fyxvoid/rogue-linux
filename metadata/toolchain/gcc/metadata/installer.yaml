steps:
  - |
      echo "[COGMAN] Building GCC (pass 2), sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing GCC (pass 2) into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
