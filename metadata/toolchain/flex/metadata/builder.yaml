steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting flex source, sir."
      tar -xf tar/flex-2.6.4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring flex, sir."
      cd build
      ../source/flex-2.6.4/configure --prefix=/usr \
        

