steps:
  - |
      echo "[COGMAN] Building gdisk, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gdisk into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
