name: gdisk
version: "1.0.10"
category: system

summary: >
  GPT fdisk partitioning tools for GUID partition tables

source:
  kind: tarball
  file: gdisk-1.0.10.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/ncurses
