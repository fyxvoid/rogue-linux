steps:
  - |
      echo "[COGMAN] Building lvm2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing lvm2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
