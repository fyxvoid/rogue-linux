name: htop
version: "3.3.0"
category: system

summary: >
  Interactive process viewer and system monitor

source:
  kind: tarball
  file: htop-3.3.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/ncurses
