steps:
  - |
      echo "[COGMAN] Building htop, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing htop into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
