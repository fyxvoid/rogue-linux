name: ltrace
version: "0.7.3"
category: system

summary: >
  Library call tracer for dynamic library debugging

source:
  kind: tarball
  file: ltrace-0.7.3.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/libelf
