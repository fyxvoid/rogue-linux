steps:
  - |
      echo "[COGMAN] Building ltrace, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ltrace into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
