steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ltrace source, sir."
      tar -xf tar/ltrace-0.7.3.tar.xz -C source

  - |
      echo "[COGMAN] Configuring ltrace, sir."
      cd build
      ../source/ltrace-0.7.3/configure --prefix=/usr \
        

