steps:
  - |
      echo "[COGMAN] Building btrfs-progs, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing btrfs-progs into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
