steps:
  - |
      echo "[COGMAN] Building parted, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing parted into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
