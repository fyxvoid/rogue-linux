name: at
version: "3.2.5"
category: system

summary: >
  Job scheduler for one-time command execution

source:
  kind: tarball
  file: at-3.2.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/linux-pam
