name: strace
version: "6.7"
category: system

summary: >
  System call tracer for debugging and diagnostic purposes

source:
  kind: tarball
  file: strace-6.7.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/libelf
