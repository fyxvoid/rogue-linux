name: squashfs-tools
version: "4.6.1"
category: system

summary: >
  SquashFS compressed filesystem creation and extraction tools

source:
  kind: tarball
  file: squashfs-tools-4.6.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/compression/zlib
    - libs/compression/xz
    - libs/compression/lz4
    - libs/compression/zstd
