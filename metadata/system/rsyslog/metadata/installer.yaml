steps:
  - |
      echo "[COGMAN] Building rsyslog, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rsyslog into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
