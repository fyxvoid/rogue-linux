steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting xfsprogs source, sir."
      tar -xf tar/xfsprogs-6.6.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring xfsprogs, sir."
      cd build
      ../source/xfsprogs-6.6.0/configure --prefix=/usr \
        

