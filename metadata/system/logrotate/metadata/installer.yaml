steps:
  - |
      echo "[COGMAN] Building logrotate, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing logrotate into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
