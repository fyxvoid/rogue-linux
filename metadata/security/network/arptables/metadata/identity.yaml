name: arptables
version: "0.0.5"
category: security/network

summary: >
  ARP table administration for filtering ARP packets

source:
  kind: tarball
  file: arptables-0.0.5.tar.xz

depends:
  build:
    - toolchain/gcc
