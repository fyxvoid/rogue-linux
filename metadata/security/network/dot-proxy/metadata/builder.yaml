steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting dot-proxy source, sir."
      tar -xf tar/dot-proxy-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building dot-proxy, sir."
      cd source/dot-proxy-1.0
      make -j$(nproc)

