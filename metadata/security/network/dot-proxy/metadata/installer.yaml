steps:
  - |
      echo "[COGMAN] Building dot-proxy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dot-proxy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
