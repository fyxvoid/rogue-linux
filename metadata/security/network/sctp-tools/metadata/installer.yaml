steps:
  - |
      echo "[COGMAN] Building sctp-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing sctp-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
