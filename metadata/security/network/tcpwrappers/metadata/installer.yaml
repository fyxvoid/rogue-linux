steps:
  - |
      echo "[COGMAN] Building tcpwrappers, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcpwrappers into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
