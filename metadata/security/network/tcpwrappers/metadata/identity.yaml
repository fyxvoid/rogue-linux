name: tcpwrappers
version: "7.6"
category: security/network

summary: >
  Host-based TCP access control for network services

source:
  kind: tarball
  file: tcpwrappers-7.6.tar.xz

depends:
  build:
    - toolchain/gcc
