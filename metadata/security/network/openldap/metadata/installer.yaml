steps:
  - |
      echo "[COGMAN] Building openldap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openldap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
