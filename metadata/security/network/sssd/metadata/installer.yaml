steps:
  - |
      echo "[COGMAN] Building sssd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing sssd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
