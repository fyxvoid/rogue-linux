steps:
  - |
      echo "[COGMAN] Building nping, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nping into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
