name: nping
version: "0.7.95"
category: security/network

summary: >
  Network packet generation tool for testing and debugging

source:
  kind: tarball
  file: nping-0.7.95.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - libs/crypto/openssl
