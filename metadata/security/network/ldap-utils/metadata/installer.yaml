steps:
  - |
      echo "[COGMAN] Building ldap-utils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ldap-utils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
