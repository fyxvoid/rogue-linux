steps:
  - |
      echo "[COGMAN] Building tcpslice, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcpslice into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
