steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting tcpslice source, sir."
      tar -xf tar/tcpslice-1.5.tar.xz -C source

  - |
      echo "[COGMAN] Configuring tcpslice, sir."
      cd build
      ../source/tcpslice-1.5/configure --prefix=/usr \
        

