name: dante
version: "1.4.3"
category: security/network

summary: >
  SOCKS server and client for secure proxy connections

source:
  kind: tarball
  file: dante-1.4.3.tar.xz

depends:
  build:
    - toolchain/gcc
