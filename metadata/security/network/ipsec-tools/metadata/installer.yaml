steps:
  - |
      echo "[COGMAN] Building ipsec-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ipsec-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
