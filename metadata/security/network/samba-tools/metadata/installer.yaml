steps:
  - |
      echo "[COGMAN] Building samba-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing samba-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
