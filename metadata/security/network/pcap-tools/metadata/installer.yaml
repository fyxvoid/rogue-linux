steps:
  - |
      echo "[COGMAN] Building pcap-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pcap-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
