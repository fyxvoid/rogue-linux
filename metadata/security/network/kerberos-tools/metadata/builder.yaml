steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting kerberos-tools source, sir."
      tar -xf tar/kerberos-tools-1.21.2.tar.xz -C source

  - |
      echo "[COGMAN] Configuring kerberos-tools, sir."
      cd build
      ../source/kerberos-tools-1.21.2/configure --prefix=/usr \
        

