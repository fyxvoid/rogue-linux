steps:
  - |
      echo "[COGMAN] Building ebtables, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ebtables into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
