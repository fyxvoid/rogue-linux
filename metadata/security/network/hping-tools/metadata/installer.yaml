steps:
  - |
      echo "[COGMAN] Building hping-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hping-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
