name: torsocks
version: "2.4.0"
category: security/network

summary: >
  Transparent SOCKS proxy for routing traffic through Tor

source:
  kind: tarball
  file: torsocks-2.4.0.tar.xz

depends:
  build:
    - toolchain/gcc
