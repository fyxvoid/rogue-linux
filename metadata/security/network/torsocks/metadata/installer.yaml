steps:
  - |
      echo "[COGMAN] Building torsocks, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing torsocks into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
