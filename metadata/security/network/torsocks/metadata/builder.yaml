steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting torsocks source, sir."
      tar -xf tar/torsocks-2.4.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring torsocks, sir."
      cd build
      ../source/torsocks-2.4.0/configure --prefix=/usr \
        

