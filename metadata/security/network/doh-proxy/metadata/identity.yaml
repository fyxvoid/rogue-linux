name: doh-proxy
version: "0.0.3"
category: security/network

summary: >
  DNS over HTTPS proxy for encrypted DNS resolution

source:
  kind: tarball
  file: doh-proxy-0.0.3.tar.xz

depends:
  build:
    - scripting/go/go
