steps:
  - |
      echo "[COGMAN] Building tor, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tor into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
