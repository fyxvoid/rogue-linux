steps:
  - |
      echo "[COGMAN] Building ipset, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ipset into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
