steps:
  - |
      echo "[COGMAN] Building freeradius, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing freeradius into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
