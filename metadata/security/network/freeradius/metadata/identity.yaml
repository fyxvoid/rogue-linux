name: freeradius
version: "3.2.3"
category: security/network

summary: >
  RADIUS authentication server for network access control

source:
  kind: tarball
  file: freeradius-3.2.3.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/general/sqlite
