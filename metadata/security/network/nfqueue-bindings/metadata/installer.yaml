steps:
  - |
      echo "[COGMAN] Building nfqueue-bindings, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nfqueue-bindings into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
