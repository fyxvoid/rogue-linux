name: nfqueue-bindings
version: "0.6"
category: security/network

summary: >
  NFQueue bindings for packet processing in userspace

source:
  kind: tarball
  file: nfqueue-bindings-0.6.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libnfnetlink
