steps:
  - |
      echo "[COGMAN] Building privoxy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing privoxy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
