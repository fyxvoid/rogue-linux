steps:
  - |
      echo "[COGMAN] Building snort-rules, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing snort-rules into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
