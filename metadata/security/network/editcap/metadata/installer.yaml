steps:
  - |
      echo "[COGMAN] Building editcap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing editcap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
