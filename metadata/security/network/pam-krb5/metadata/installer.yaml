steps:
  - |
      echo "[COGMAN] Building pam-krb5, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pam-krb5 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
