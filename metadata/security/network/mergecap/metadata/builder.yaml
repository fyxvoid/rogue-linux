steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting mergecap source, sir."
      tar -xf tar/mergecap-4.2.3.tar.xz -C source

  - |
      echo "[COGMAN] Building mergecap, sir."
      cd source/mergecap-4.2.3
      make -j$(nproc)

