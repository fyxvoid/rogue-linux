steps:
  - |
      echo "[COGMAN] Building mergecap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mergecap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
