steps:
  - |
      echo "[COGMAN] Building conntrack-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing conntrack-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
