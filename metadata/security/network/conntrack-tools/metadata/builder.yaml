steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting conntrack-tools source, sir."
      tar -xf tar/conntrack-tools-1.4.8.tar.xz -C source

  - |
      echo "[COGMAN] Configuring conntrack-tools, sir."
      cd build
      ../source/conntrack-tools-1.4.8/configure --prefix=/usr \
        

