steps:
  - |
      echo "[COGMAN] Building openvpn-sec, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openvpn-sec into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
