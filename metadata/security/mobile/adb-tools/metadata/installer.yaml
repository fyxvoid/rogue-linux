steps:
  - |
      echo "[COGMAN] Building adb-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing adb-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
