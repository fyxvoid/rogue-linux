steps:
  - |
      echo "[COGMAN] Building simplify, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing simplify into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
