steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting reflutter source, sir."
      tar -xf tar/reflutter-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building reflutter Python package, sir."
      cd source/reflutter-1.0
      python3 setup.py build

