steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting libimobiledevice source, sir."
      tar -xf tar/libimobiledevice-1.3.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring libimobiledevice, sir."
      cd build
      ../source/libimobiledevice-1.3.0/configure --prefix=/usr \
        

