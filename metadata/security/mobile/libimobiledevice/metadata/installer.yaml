steps:
  - |
      echo "[COGMAN] Building libimobiledevice, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libimobiledevice into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
