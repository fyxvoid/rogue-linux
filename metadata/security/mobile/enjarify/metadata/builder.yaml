steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting enjarify source, sir."
      tar -xf tar/enjarify-1.0.3.tar.xz -C source

  - |
      echo "[COGMAN] Building enjarify Python package, sir."
      cd source/enjarify-1.0.3
      python3 setup.py build

