steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ios-deploy source, sir."
      tar -xf tar/ios-deploy-1.12.2.tar.xz -C source

  - |
      echo "[COGMAN] Building ios-deploy, sir."
      cd source/ios-deploy-1.12.2
      make -j$(nproc)

