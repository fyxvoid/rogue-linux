name: ios-deploy
version: "1.12.2"
category: security/mobile

summary: >
  Install and debug iOS apps from the command line

source:
  kind: tarball
  file: ios-deploy-1.12.2.tar.xz

depends:
  build:
    - toolchain/gcc
