steps:
  - |
      echo "[COGMAN] Building usbmuxd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing usbmuxd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
