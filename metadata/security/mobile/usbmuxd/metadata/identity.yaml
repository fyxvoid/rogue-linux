name: usbmuxd
version: "1.1.1"
category: security/mobile

summary: >
  USB multiplexing daemon for iOS device communication

source:
  kind: tarball
  file: usbmuxd-1.1.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/general/libusb
