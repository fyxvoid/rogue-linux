steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting qark source, sir."
      tar -xf tar/qark-4.0.0.tar.xz -C source

  - |
      echo "[COGMAN] Building qark Python package, sir."
      cd source/qark-4.0.0
      python3 setup.py build

