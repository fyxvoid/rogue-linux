steps:
  - |
      echo "[COGMAN] Building ideviceinstaller, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ideviceinstaller into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
