steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ideviceinstaller source, sir."
      tar -xf tar/ideviceinstaller-1.1.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring ideviceinstaller, sir."
      cd build
      ../source/ideviceinstaller-1.1.1/configure --prefix=/usr \
        

