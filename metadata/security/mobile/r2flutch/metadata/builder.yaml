steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting r2flutch source, sir."
      tar -xf tar/r2flutch-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building r2flutch Python package, sir."
      cd source/r2flutch-1.0
      python3 setup.py build

