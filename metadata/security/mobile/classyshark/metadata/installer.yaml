steps:
  - |
      echo "[COGMAN] Building classyshark, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing classyshark into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
