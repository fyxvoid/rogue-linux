steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting pidcat source, sir."
      tar -xf tar/pidcat-2.1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building pidcat Python package, sir."
      cd source/pidcat-2.1.0
      python3 setup.py build

