name: clutch
version: "2.0.4"
category: security/mobile

summary: >
  High-speed iOS decryption tool for dumping app binaries

source:
  kind: tarball
  file: clutch-2.0.4.tar.xz

depends:
  build:
    - toolchain/gcc
