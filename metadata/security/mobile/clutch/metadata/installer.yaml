steps:
  - |
      echo "[COGMAN] Building clutch, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing clutch into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
