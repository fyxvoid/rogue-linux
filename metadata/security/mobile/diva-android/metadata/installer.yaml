steps:
  - |
      echo "[COGMAN] Building diva-android, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing diva-android into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
