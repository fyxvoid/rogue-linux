steps:
  - |
      echo "[COGMAN] Building vulscan, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing vulscan into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
