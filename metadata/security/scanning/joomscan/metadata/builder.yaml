steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting joomscan source, sir."
      tar -xf tar/joomscan-0.0.7.tar.xz -C source

  - |
      echo "[COGMAN] Building joomscan, sir."
      cd source/joomscan-0.0.7
      make -j$(nproc)

