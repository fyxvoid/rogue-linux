steps:
  - |
      echo "[COGMAN] Building testssl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing testssl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
