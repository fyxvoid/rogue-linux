steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting testssl source, sir."
      tar -xf tar/testssl-3.0.8.tar.xz -C source

  - |
      echo "[COGMAN] Building testssl, sir."
      cd source/testssl-3.0.8
      make -j$(nproc)

