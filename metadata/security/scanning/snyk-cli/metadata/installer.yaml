steps:
  - |
      echo "[COGMAN] Building snyk-cli, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing snyk-cli into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
