steps:
  - |
      echo "[COGMAN] Building chkrootkit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing chkrootkit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
