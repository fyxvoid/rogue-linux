name: rkhunter
version: "1.4.6"
category: security/scanning

summary: >
  Rootkit Hunter scans for rootkits backdoors and local exploits

source:
  kind: tarball
  file: rkhunter-1.4.6.tar.xz

depends:
  build:
    - base/bash
    - toolchain/perl
