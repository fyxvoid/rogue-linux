steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting wafw00f source, sir."
      tar -xf tar/wafw00f-2.2.0.tar.xz -C source

  - |
      echo "[COGMAN] Building wafw00f Python package, sir."
      cd source/wafw00f-2.2.0
      python3 setup.py build

