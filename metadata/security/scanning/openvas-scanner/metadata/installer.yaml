steps:
  - |
      echo "[COGMAN] Building openvas-scanner, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openvas-scanner into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
