steps:
  - |
      echo "[COGMAN] Building npm-audit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing npm-audit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
