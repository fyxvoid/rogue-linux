name: kube-bench
version: "0.7.2"
category: security/scanning

summary: >
  CIS Kubernetes Benchmark checker for cluster hardening

source:
  kind: tarball
  file: kube-bench-0.7.2.tar.xz

depends:
  build:
    - scripting/go/go
