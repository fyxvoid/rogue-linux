steps:
  - |
      echo "[COGMAN] Building graudit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing graudit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
