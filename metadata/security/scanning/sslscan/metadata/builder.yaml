steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting sslscan source, sir."
      tar -xf tar/sslscan-2.1.1.tar.xz -C source

  - |
      echo "[COGMAN] Building sslscan, sir."
      cd source/sslscan-2.1.1
      make -j$(nproc)

