steps:
  - |
      echo "[COGMAN] Building sslscan, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing sslscan into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
