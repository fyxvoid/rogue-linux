steps:
  - |
      echo "[COGMAN] Building lynis, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing lynis into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
