steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting nikto source, sir."
      tar -xf tar/nikto-2.5.0.tar.xz -C source

  - |
      echo "[COGMAN] Building nikto, sir."
      cd source/nikto-2.5.0
      make -j$(nproc)

