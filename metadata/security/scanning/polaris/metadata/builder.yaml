steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting polaris source, sir."
      tar -xf tar/polaris-9.0.1.tar.xz -C source

  - |
      echo "[COGMAN] Building polaris with Go, sir."
      cd source/polaris-9.0.1
      go build -o ../../build/ ./...

