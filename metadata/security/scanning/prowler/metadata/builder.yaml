steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting prowler source, sir."
      tar -xf tar/prowler-4.0.1.tar.xz -C source

  - |
      echo "[COGMAN] Building prowler Python package, sir."
      cd source/prowler-4.0.1
      python3 setup.py build

