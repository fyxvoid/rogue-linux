steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bandit source, sir."
      tar -xf tar/bandit-1.7.7.tar.xz -C source

  - |
      echo "[COGMAN] Building bandit Python package, sir."
      cd source/bandit-1.7.7
      python3 setup.py build

