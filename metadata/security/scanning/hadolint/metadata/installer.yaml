steps:
  - |
      echo "[COGMAN] Building hadolint, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hadolint into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
