steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting hadolint source, sir."
      tar -xf tar/hadolint-2.12.0.tar.xz -C source

  - |
      echo "[COGMAN] Building hadolint, sir."
      cd source/hadolint-2.12.0
      make -j$(nproc)

