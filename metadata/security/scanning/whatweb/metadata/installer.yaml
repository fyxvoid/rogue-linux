steps:
  - |
      echo "[COGMAN] Building whatweb, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing whatweb into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
