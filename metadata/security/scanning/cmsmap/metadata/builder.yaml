steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting cmsmap source, sir."
      tar -xf tar/cmsmap-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building cmsmap Python package, sir."
      cd source/cmsmap-1.0
      python3 setup.py build

