name: bundler-audit
version: "0.9.1"
category: security/scanning

summary: >
  Patch-level verification for Ruby Bundler dependencies

source:
  kind: tarball
  file: bundler-audit-0.9.1.tar.xz

depends:
  build:
    - scripting/ruby/ruby
    - scripting/ruby/bundler
