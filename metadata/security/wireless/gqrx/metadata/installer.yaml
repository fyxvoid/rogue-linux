steps:
  - |
      echo "[COGMAN] Building gqrx, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gqrx into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
