steps:
  - |
      echo "[COGMAN] Building asleap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing asleap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
