name: kalibrate-rtl
version: "0.4.1"
category: security/wireless

summary: >
  GSM base station frequency scanner using RTL-SDR

source:
  kind: tarball
  file: kalibrate-rtl-0.4.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - security/wireless/rtl-sdr
