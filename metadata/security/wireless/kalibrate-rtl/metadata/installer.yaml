steps:
  - |
      echo "[COGMAN] Building kalibrate-rtl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing kalibrate-rtl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
