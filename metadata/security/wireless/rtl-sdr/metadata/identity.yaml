name: rtl-sdr
version: "2.0.1"
category: security/wireless

summary: >
  RTL-SDR USB dongle software for radio reception

source:
  kind: tarball
  file: rtl-sdr-2.0.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
    - libs/general/libusb
