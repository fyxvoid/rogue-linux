steps:
  - |
      echo "[COGMAN] Building rtl-sdr, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rtl-sdr into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
