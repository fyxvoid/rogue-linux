steps:
  - |
      echo "[COGMAN] Building kismet, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing kismet into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
