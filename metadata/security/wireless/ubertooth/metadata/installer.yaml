steps:
  - |
      echo "[COGMAN] Building ubertooth, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ubertooth into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
