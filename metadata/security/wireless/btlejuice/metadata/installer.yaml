steps:
  - |
      echo "[COGMAN] Building btlejuice, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing btlejuice into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
