steps:
  - |
      echo "[COGMAN] Building gnuradio, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gnuradio into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
