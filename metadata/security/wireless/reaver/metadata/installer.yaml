steps:
  - |
      echo "[COGMAN] Building reaver, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing reaver into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
