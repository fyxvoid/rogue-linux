steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting reaver source, sir."
      tar -xf tar/reaver-1.6.6.tar.xz -C source

  - |
      echo "[COGMAN] Configuring reaver, sir."
      cd build
      ../source/reaver-1.6.6/configure --prefix=/usr \
        

