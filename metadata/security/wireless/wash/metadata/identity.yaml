name: wash
version: "1.6.6"
category: security/wireless

summary: >
  Scan for WPS-enabled access points

source:
  kind: tarball
  file: wash-1.6.6.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
