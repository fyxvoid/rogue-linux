steps:
  - |
      echo "[COGMAN] Building hostapd-wpe, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hostapd-wpe into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
