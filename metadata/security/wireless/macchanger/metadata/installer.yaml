steps:
  - |
      echo "[COGMAN] Building macchanger, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing macchanger into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
