name: macchanger
version: "1.7.0"
category: security/wireless

summary: >
  MAC address spoofing utility

source:
  kind: tarball
  file: macchanger-1.7.0.tar.xz

depends:
  build:
    - toolchain/gcc
