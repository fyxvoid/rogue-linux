steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting macchanger source, sir."
      tar -xf tar/macchanger-1.7.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring macchanger, sir."
      cd build
      ../source/macchanger-1.7.0/configure --prefix=/usr \
        

