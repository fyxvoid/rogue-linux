steps:
  - |
      echo "[COGMAN] Building iw-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing iw-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
