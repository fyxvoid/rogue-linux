steps:
  - |
      echo "[COGMAN] Building spooftooph, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing spooftooph into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
