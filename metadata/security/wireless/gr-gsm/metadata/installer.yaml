steps:
  - |
      echo "[COGMAN] Building gr-gsm, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gr-gsm into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
