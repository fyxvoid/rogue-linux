name: pixiewps
version: "1.4.2"
category: security/wireless

summary: >
  Offline WPS PIN brute force tool exploiting low entropy

source:
  kind: tarball
  file: pixiewps-1.4.2.tar.xz

depends:
  build:
    - toolchain/gcc
