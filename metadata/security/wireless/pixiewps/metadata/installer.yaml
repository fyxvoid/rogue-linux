steps:
  - |
      echo "[COGMAN] Building pixiewps, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pixiewps into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
