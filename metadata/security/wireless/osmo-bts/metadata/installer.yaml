steps:
  - |
      echo "[COGMAN] Building osmo-bts, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing osmo-bts into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
