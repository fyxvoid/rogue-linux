steps:
  - |
      echo "[COGMAN] Building srslte, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing srslte into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
