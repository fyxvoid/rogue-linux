steps:
  - |
      echo "[COGMAN] Building hackrf-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hackrf-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
