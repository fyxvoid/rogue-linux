steps:
  - |
      echo "[COGMAN] Building wavemon, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wavemon into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
