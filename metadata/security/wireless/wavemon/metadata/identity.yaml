name: wavemon
version: "0.9.5"
category: security/wireless

summary: >
  Wireless network monitor for curses terminal

source:
  kind: tarball
  file: wavemon-0.9.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libnl
    - base/ncurses
