steps:
  - |
      echo "[COGMAN] Building multimon-ng, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing multimon-ng into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
