name: wifi-honey
version: "1.0"
category: security/wireless

summary: >
  Create fake access points for client connection monitoring

source:
  kind: tarball
  file: wifi-honey-1.0.tar.xz

depends:
  build:
    - base/bash
    - security/wireless/aircrack-ng
