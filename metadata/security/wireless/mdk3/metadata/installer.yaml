steps:
  - |
      echo "[COGMAN] Building mdk3, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mdk3 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
