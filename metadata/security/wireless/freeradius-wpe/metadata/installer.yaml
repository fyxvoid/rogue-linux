steps:
  - |
      echo "[COGMAN] Building freeradius-wpe, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing freeradius-wpe into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
