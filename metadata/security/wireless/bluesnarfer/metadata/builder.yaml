steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bluesnarfer source, sir."
      tar -xf tar/bluesnarfer-0.1.tar.xz -C source

  - |
      echo "[COGMAN] Building bluesnarfer, sir."
      cd source/bluesnarfer-0.1
      make -j$(nproc)

