name: bluesnarfer
version: "0.1"
category: security/wireless

summary: >
  Bluetooth attack tool for sniffing contact information

source:
  kind: tarball
  file: bluesnarfer-0.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/wireless/bluez
