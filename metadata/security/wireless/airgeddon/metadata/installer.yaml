steps:
  - |
      echo "[COGMAN] Building airgeddon, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing airgeddon into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
