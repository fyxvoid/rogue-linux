steps:
  - |
      echo "[COGMAN] Building xorsearch, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing xorsearch into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
