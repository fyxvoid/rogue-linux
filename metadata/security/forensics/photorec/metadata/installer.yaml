steps:
  - |
      echo "[COGMAN] Building photorec, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing photorec into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
