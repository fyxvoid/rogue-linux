steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting photorec source, sir."
      tar -xf tar/photorec-7.2.tar.xz -C source

  - |
      echo "[COGMAN] Configuring photorec, sir."
      cd build
      ../source/photorec-7.2/configure --prefix=/usr \
        

