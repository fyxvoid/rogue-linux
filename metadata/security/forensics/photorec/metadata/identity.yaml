name: photorec
version: "7.2"
category: security/forensics

summary: >
  File data recovery utility for hard disks and digital cameras

source:
  kind: tarball
  file: photorec-7.2.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/ncurses
