steps:
  - |
      echo "[COGMAN] Building safecopy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing safecopy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
