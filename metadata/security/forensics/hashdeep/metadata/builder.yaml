steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting hashdeep source, sir."
      tar -xf tar/hashdeep-4.4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring hashdeep, sir."
      cd build
      ../source/hashdeep-4.4/configure --prefix=/usr \
        

