steps:
  - |
      echo "[COGMAN] Building hashdeep, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hashdeep into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
