steps:
  - |
      echo "[COGMAN] Building outguess, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing outguess into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
