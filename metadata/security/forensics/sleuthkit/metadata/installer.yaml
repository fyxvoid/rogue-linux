steps:
  - |
      echo "[COGMAN] Building sleuthkit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing sleuthkit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
