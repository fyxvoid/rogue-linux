steps:
  - |
      echo "[COGMAN] Building unhide, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing unhide into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
