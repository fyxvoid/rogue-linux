steps:
  - |
      echo "[COGMAN] Building guymager, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing guymager into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
