steps:
  - |
      echo "[COGMAN] Building foremost, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing foremost into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
