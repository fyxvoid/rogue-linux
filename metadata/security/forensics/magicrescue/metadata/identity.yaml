name: magicrescue
version: "1.1.10"
category: security/forensics

summary: >
  File carver for recovering files based on magic bytes

source:
  kind: tarball
  file: magicrescue-1.1.10.tar.xz

depends:
  build:
    - toolchain/gcc
