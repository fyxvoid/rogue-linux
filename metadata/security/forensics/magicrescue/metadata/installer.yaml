steps:
  - |
      echo "[COGMAN] Building magicrescue, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing magicrescue into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
