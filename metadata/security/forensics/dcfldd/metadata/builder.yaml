steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting dcfldd source, sir."
      tar -xf tar/dcfldd-1.9.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring dcfldd, sir."
      cd build
      ../source/dcfldd-1.9.1/configure --prefix=/usr \
        

