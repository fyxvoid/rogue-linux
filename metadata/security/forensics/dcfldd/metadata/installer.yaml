steps:
  - |
      echo "[COGMAN] Building dcfldd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dcfldd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
