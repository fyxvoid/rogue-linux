steps:
  - |
      echo "[COGMAN] Building ddrescue, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ddrescue into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
