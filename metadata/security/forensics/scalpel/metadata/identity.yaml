name: scalpel
version: "2.0"
category: security/forensics

summary: >
  Fast filesystem-independent file carver for data recovery

source:
  kind: tarball
  file: scalpel-2.0.tar.xz

depends:
  build:
    - toolchain/gcc
