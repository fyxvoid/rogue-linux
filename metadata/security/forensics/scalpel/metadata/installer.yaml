steps:
  - |
      echo "[COGMAN] Building scalpel, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing scalpel into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
