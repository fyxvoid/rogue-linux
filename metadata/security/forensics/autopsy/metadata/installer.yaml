steps:
  - |
      echo "[COGMAN] Building autopsy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing autopsy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
