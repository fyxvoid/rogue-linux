steps:
  - |
      echo "[COGMAN] Building ewftools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ewftools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
