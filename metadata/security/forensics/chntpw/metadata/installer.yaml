steps:
  - |
      echo "[COGMAN] Building chntpw, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing chntpw into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
