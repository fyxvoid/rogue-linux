steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting chntpw source, sir."
      tar -xf tar/chntpw-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building chntpw, sir."
      cd source/chntpw-1.0
      make -j$(nproc)

