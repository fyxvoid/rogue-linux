name: chntpw
version: "1.0"
category: security/forensics

summary: >
  Offline Windows SAM password editor and registry tool

source:
  kind: tarball
  file: chntpw-1.0.tar.xz

depends:
  build:
    - toolchain/gcc
