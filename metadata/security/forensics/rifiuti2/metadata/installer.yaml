steps:
  - |
      echo "[COGMAN] Building rifiuti2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rifiuti2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
