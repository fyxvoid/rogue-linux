steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting rifiuti2 source, sir."
      tar -xf tar/rifiuti2-0.7.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring rifiuti2, sir."
      cd build
      ../source/rifiuti2-0.7.0/configure --prefix=/usr \
        

