name: bulk-extractor
version: "2.0.5"
category: security/forensics

summary: >
  High-performance digital forensics feature extraction tool

source:
  kind: tarball
  file: bulk-extractor-2.0.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
    - libs/crypto/openssl
