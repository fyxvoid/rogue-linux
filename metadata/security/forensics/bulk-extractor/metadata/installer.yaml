steps:
  - |
      echo "[COGMAN] Building bulk-extractor, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing bulk-extractor into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
