steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting oledump source, sir."
      tar -xf tar/oledump-0.0.75.tar.xz -C source

  - |
      echo "[COGMAN] Building oledump Python package, sir."
      cd source/oledump-0.0.75
      python3 setup.py build

