steps:
  - |
      echo "[COGMAN] Building cyberchef-cli, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cyberchef-cli into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
