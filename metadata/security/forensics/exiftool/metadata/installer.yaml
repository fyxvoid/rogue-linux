steps:
  - |
      echo "[COGMAN] Building exiftool, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing exiftool into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
