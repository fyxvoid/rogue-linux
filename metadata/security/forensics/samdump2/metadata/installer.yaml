steps:
  - |
      echo "[COGMAN] Building samdump2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing samdump2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
