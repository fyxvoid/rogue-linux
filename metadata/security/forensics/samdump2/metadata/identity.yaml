name: samdump2
version: "3.0.0"
category: security/forensics

summary: >
  Dump Windows password hashes from SAM and SYSTEM files

source:
  kind: tarball
  file: samdump2-3.0.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
