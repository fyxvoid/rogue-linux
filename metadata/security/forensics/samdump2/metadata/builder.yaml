steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting samdump2 source, sir."
      tar -xf tar/samdump2-3.0.0.tar.xz -C source

  - |
      echo "[COGMAN] Building samdump2, sir."
      cd source/samdump2-3.0.0
      make -j$(nproc)

