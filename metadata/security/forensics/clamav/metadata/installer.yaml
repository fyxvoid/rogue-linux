steps:
  - |
      echo "[COGMAN] Building clamav, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing clamav into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
