name: chainsaw
version: "2.9.1"
category: security/forensics

summary: >
  Rapidly search and hunt through Windows forensic artifacts

source:
  kind: tarball
  file: chainsaw-2.9.1.tar.xz

depends:
  build:
    - scripting/rust/rust
