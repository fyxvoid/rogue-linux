steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting chainsaw source, sir."
      tar -xf tar/chainsaw-2.9.1.tar.xz -C source

  - |
      echo "[COGMAN] Building chainsaw with Cargo, sir."
      cd source/chainsaw-2.9.1
      cargo build --release

