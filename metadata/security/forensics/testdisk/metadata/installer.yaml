steps:
  - |
      echo "[COGMAN] Building testdisk, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing testdisk into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
