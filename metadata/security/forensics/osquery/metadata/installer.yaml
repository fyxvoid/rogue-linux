steps:
  - |
      echo "[COGMAN] Building osquery, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing osquery into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
