steps:
  - |
      echo "[COGMAN] Building ext4magic, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ext4magic into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
