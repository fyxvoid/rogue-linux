name: regripper
version: "3.0"
category: security/forensics

summary: >
  Windows registry data extraction and analysis tool

source:
  kind: tarball
  file: regripper-3.0.tar.xz

depends:
  build:
    - toolchain/perl
