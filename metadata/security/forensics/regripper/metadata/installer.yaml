steps:
  - |
      echo "[COGMAN] Building regripper, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing regripper into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
