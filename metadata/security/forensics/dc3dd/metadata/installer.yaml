steps:
  - |
      echo "[COGMAN] Building dc3dd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dc3dd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
