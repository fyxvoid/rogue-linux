steps:
  - |
      echo "[COGMAN] Building stegseek, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing stegseek into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
