steps:
  - |
      echo "[COGMAN] Building openstego, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openstego into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
