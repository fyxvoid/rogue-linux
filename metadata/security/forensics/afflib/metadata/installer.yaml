steps:
  - |
      echo "[COGMAN] Building afflib, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing afflib into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
