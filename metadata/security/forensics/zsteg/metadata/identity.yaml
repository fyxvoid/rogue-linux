name: zsteg
version: "0.2.13"
category: security/forensics

summary: >
  Detect steganography in PNG and BMP image files

source:
  kind: tarball
  file: zsteg-0.2.13.tar.xz

depends:
  build:
    - scripting/ruby/ruby
