steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting zsteg source, sir."
      tar -xf tar/zsteg-0.2.13.tar.xz -C source

  - |
      echo "[COGMAN] Building zsteg, sir."
      cd source/zsteg-0.2.13
      make -j$(nproc)

