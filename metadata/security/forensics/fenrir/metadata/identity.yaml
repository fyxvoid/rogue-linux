name: fenrir
version: "0.9.0"
category: security/forensics

summary: >
  Simple Bash IOC scanner for Linux systems

source:
  kind: tarball
  file: fenrir-0.9.0.tar.xz

depends:
  build:
    - base/bash
    - base/grep
