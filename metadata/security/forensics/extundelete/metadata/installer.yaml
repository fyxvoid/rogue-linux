steps:
  - |
      echo "[COGMAN] Building extundelete, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing extundelete into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
