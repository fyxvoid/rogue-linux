steps:
  - |
      echo "[COGMAN] Building amap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing amap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
