name: amap
version: "5.4"
category: security/recon

summary: >
  Application protocol detection tool for service identification

source:
  kind: tarball
  file: amap-5.4.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - networking/core/libpcap
