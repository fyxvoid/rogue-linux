steps:
  - |
      echo "[COGMAN] Building xprobe2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing xprobe2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
