steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting xprobe2 source, sir."
      tar -xf tar/xprobe2-0.3.tar.xz -C source

  - |
      echo "[COGMAN] Configuring xprobe2, sir."
      cd build
      ../source/xprobe2-0.3/configure --prefix=/usr \
        

