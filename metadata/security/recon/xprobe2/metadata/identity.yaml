name: xprobe2
version: "0.3"
category: security/recon

summary: >
  Remote active operating system fingerprinting tool

source:
  kind: tarball
  file: xprobe2-0.3.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
