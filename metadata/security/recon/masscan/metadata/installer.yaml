steps:
  - |
      echo "[COGMAN] Building masscan, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing masscan into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
