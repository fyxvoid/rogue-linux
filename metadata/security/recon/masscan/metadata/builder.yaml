steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting masscan source, sir."
      tar -xf tar/masscan-1.3.2.tar.xz -C source

  - |
      echo "[COGMAN] Building masscan, sir."
      cd source/masscan-1.3.2
      make -j$(nproc)

