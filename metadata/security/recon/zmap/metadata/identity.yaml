name: zmap
version: "4.1.0"
category: security/recon

summary: >
  Internet-wide network scanner for fast single-port surveys

source:
  kind: tarball
  file: zmap-4.1.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
    - networking/core/libpcap
    - libs/general/json-c
