steps:
  - |
      echo "[COGMAN] Building zmap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing zmap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
