name: shuffledns
version: "1.1.0"
category: security/recon

summary: >
  Wrapper around massdns for active brute forcing of DNS records

source:
  kind: tarball
  file: shuffledns-1.1.0.tar.xz

depends:
  build:
    - scripting/go/go
