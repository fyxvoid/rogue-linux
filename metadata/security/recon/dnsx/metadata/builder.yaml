steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting dnsx source, sir."
      tar -xf tar/dnsx-1.2.1.tar.xz -C source

  - |
      echo "[COGMAN] Building dnsx with Go, sir."
      cd source/dnsx-1.2.1
      go build -o ../../build/ ./...

