name: dnswalk
version: "2.0.2"
category: security/recon

summary: >
  DNS debugging tool that performs zone transfers and checks records

source:
  kind: tarball
  file: dnswalk-2.0.2.tar.xz

depends:
  build:
    - toolchain/perl
