steps:
  - |
      echo "[COGMAN] Building dnswalk, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dnswalk into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
