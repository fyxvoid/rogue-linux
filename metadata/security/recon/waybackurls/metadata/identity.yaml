name: waybackurls
version: "0.1.0"
category: security/recon

summary: >
  Fetch URLs from the Wayback Machine for a given domain

source:
  kind: tarball
  file: waybackurls-0.1.0.tar.xz

depends:
  build:
    - scripting/go/go
