steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting httpx source, sir."
      tar -xf tar/httpx-1.6.0.tar.xz -C source

  - |
      echo "[COGMAN] Building httpx with Go, sir."
      cd source/httpx-1.6.0
      go build -o ../../build/ ./...

