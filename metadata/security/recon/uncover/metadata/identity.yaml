name: uncover
version: "1.0.7"
category: security/recon

summary: >
  Quickly discover exposed hosts using multiple search engines

source:
  kind: tarball
  file: uncover-1.0.7.tar.xz

depends:
  build:
    - scripting/go/go
