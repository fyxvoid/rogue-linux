steps:
  - |
      echo "[COGMAN] Building libnet, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libnet into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
