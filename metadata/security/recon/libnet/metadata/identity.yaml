name: libnet
version: "1.2"
category: security/recon

summary: >
  Low-level network packet construction library

source:
  kind: tarball
  file: libnet-1.2.tar.xz

depends:
  build:
    - toolchain/gcc
