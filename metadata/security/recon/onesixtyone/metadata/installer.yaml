steps:
  - |
      echo "[COGMAN] Building onesixtyone, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing onesixtyone into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
