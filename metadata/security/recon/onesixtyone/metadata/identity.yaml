name: onesixtyone
version: "0.3.3"
category: security/recon

summary: >
  Fast SNMP community string scanner

source:
  kind: tarball
  file: onesixtyone-0.3.3.tar.xz

depends:
  build:
    - toolchain/gcc
