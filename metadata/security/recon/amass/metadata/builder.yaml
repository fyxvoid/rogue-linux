steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting amass source, sir."
      tar -xf tar/amass-4.2.0.tar.xz -C source

  - |
      echo "[COGMAN] Building amass with Go, sir."
      cd source/amass-4.2.0
      go build -o ../../build/ ./...

