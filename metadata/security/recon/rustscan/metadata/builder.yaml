steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting rustscan source, sir."
      tar -xf tar/rustscan-2.2.2.tar.xz -C source

  - |
      echo "[COGMAN] Building rustscan with Cargo, sir."
      cd source/rustscan-2.2.2
      cargo build --release

