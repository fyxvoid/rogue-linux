steps:
  - |
      echo "[COGMAN] Building dnsenum, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dnsenum into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
