name: dnsenum
version: "1.3.1"
category: security/recon

summary: >
  Multithreaded DNS enumeration and zone transfer tool

source:
  kind: tarball
  file: dnsenum-1.3.1.tar.xz

depends:
  build:
    - toolchain/perl
