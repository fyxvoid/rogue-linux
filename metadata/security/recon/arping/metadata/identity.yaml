name: arping
version: "2.24"
category: security/recon

summary: >
  ARP ping utility for discovering hosts on local network

source:
  kind: tarball
  file: arping-2.24.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - networking/core/libnet
