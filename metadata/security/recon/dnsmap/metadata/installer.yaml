steps:
  - |
      echo "[COGMAN] Building dnsmap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dnsmap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
