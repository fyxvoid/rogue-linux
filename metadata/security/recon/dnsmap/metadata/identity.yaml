name: dnsmap
version: "0.36"
category: security/recon

summary: >
  DNS network mapper for subdomain brute-forcing

source:
  kind: tarball
  file: dnsmap-0.36.tar.xz

depends:
  build:
    - toolchain/gcc
