steps:
  - |
      echo "[COGMAN] Building p0f, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing p0f into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
