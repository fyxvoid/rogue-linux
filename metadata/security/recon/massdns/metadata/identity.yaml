name: massdns
version: "1.0.0"
category: security/recon

summary: >
  High-performance DNS stub resolver for bulk lookups

source:
  kind: tarball
  file: massdns-1.0.0.tar.xz

depends:
  build:
    - toolchain/gcc
