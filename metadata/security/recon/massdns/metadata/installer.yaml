steps:
  - |
      echo "[COGMAN] Building massdns, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing massdns into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
