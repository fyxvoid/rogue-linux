steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting httprobe source, sir."
      tar -xf tar/httprobe-0.2.tar.xz -C source

  - |
      echo "[COGMAN] Building httprobe with Go, sir."
      cd source/httprobe-0.2
      go build -o ../../build/ ./...

