name: arp-scan
version: "1.10.0"
category: security/recon

summary: >
  ARP scanning and fingerprinting tool for local network discovery

source:
  kind: tarball
  file: arp-scan-1.10.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
