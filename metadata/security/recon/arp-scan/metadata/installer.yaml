steps:
  - |
      echo "[COGMAN] Building arp-scan, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing arp-scan into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
