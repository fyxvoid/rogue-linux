steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting fping source, sir."
      tar -xf tar/fping-5.2.tar.xz -C source

  - |
      echo "[COGMAN] Configuring fping, sir."
      cd build
      ../source/fping-5.2/configure --prefix=/usr \
        

