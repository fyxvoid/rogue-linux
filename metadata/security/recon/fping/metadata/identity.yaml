name: fping
version: "5.2"
category: security/recon

summary: >
  Fast ping utility for parallel host reachability checking

source:
  kind: tarball
  file: fping-5.2.tar.xz

depends:
  build:
    - toolchain/gcc
