steps:
  - |
      echo "[COGMAN] Building fping, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing fping into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
