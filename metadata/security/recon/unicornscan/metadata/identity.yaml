name: unicornscan
version: "0.4.7"
category: security/recon

summary: >
  Asynchronous stateless TCP and UDP port scanner

source:
  kind: tarball
  file: unicornscan-0.4.7.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
