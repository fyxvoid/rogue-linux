steps:
  - |
      echo "[COGMAN] Building hping3, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hping3 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
