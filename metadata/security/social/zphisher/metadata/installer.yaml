steps:
  - |
      echo "[COGMAN] Building zphisher, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing zphisher into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
