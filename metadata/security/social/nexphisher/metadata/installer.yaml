steps:
  - |
      echo "[COGMAN] Building nexphisher, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nexphisher into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
