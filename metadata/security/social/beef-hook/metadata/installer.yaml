steps:
  - |
      echo "[COGMAN] Building beef-hook, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing beef-hook into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
