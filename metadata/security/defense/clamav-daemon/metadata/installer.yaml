steps:
  - |
      echo "[COGMAN] Building clamav-daemon, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing clamav-daemon into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
