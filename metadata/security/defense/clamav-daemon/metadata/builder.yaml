steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting clamav-daemon source, sir."
      tar -xf tar/clamav-daemon-1.3.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring clamav-daemon with CMake, sir."
      cd build
      cmake ../source/clamav-daemon-1.3.0 -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release

