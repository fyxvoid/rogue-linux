name: fail2ban
version: "1.1.0"
category: security/defense

summary: >
  Ban hosts that cause multiple authentication errors

source:
  kind: tarball
  file: fail2ban-1.1.0.tar.xz

depends:
  build:
    - scripting/python/python3
