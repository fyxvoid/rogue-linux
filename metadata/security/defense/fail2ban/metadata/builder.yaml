steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting fail2ban source, sir."
      tar -xf tar/fail2ban-1.1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building fail2ban Python package, sir."
      cd source/fail2ban-1.1.0
      python3 setup.py build

