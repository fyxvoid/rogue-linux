steps:
  - |
      echo "[COGMAN] Building signify, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing signify into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
