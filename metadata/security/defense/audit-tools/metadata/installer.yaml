steps:
  - |
      echo "[COGMAN] Building audit-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing audit-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
