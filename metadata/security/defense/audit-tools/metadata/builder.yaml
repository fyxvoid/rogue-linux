steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting audit-tools source, sir."
      tar -xf tar/audit-tools-3.1.4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring audit-tools, sir."
      cd build
      ../source/audit-tools-3.1.4/configure --prefix=/usr \
        --sysconfdir=/etc

