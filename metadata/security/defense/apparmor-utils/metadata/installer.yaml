steps:
  - |
      echo "[COGMAN] Building apparmor-utils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing apparmor-utils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
