steps:
  - |
      echo "[COGMAN] Building tripwire, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tripwire into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
