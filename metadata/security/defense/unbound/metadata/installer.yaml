steps:
  - |
      echo "[COGMAN] Building unbound, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing unbound into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
