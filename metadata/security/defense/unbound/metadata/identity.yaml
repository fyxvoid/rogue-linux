name: unbound
version: "1.19.1"
category: security/defense

summary: >
  Validating recursive and caching DNS resolver for security

source:
  kind: tarball
  file: unbound-1.19.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/general/libevent
