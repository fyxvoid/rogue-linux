name: sshguard
version: "2.4.3"
category: security/defense

summary: >
  Protect hosts from brute-force attacks on SSH and other services

source:
  kind: tarball
  file: sshguard-2.4.3.tar.xz

depends:
  build:
    - toolchain/gcc
