steps:
  - |
      echo "[COGMAN] Building sshguard, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing sshguard into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
