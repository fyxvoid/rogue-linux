steps:
  - |
      echo "[COGMAN] Building samhain, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing samhain into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
