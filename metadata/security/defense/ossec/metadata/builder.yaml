steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ossec source, sir."
      tar -xf tar/ossec-3.7.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring ossec, sir."
      cd build
      ../source/ossec-3.7.0/configure --prefix=/usr \
        

