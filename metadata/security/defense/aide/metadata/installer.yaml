steps:
  - |
      echo "[COGMAN] Building aide, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing aide into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
