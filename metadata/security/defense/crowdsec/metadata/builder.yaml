steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting crowdsec source, sir."
      tar -xf tar/crowdsec-1.6.0.tar.xz -C source

  - |
      echo "[COGMAN] Building crowdsec with Go, sir."
      cd source/crowdsec-1.6.0
      go build -o ../../build/ ./...

