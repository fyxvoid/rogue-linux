steps:
  - |
      echo "[COGMAN] Building wazuh-agent, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wazuh-agent into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
