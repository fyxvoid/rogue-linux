name: stubby
version: "0.4.3"
category: security/defense

summary: >
  DNS Privacy stub resolver for DNS over TLS

source:
  kind: tarball
  file: stubby-0.4.3.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
    - libs/general/libyaml
