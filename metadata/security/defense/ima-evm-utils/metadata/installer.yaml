steps:
  - |
      echo "[COGMAN] Building ima-evm-utils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ima-evm-utils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
