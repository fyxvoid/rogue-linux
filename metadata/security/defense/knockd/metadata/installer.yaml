steps:
  - |
      echo "[COGMAN] Building knockd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing knockd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
