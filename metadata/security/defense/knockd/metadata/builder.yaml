steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting knockd source, sir."
      tar -xf tar/knockd-0.8.tar.xz -C source

  - |
      echo "[COGMAN] Configuring knockd, sir."
      cd build
      ../source/knockd-0.8/configure --prefix=/usr \
        

