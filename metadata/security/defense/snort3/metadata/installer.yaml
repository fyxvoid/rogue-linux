steps:
  - |
      echo "[COGMAN] Building snort3, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing snort3 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
