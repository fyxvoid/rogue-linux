steps:
  - |
      echo "[COGMAN] Building psad, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing psad into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
