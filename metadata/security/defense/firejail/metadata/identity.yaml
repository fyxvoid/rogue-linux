name: firejail
version: "0.9.72"
category: security/defense

summary: >
  SUID sandbox program for restricting application access

source:
  kind: tarball
  file: firejail-0.9.72.tar.xz

depends:
  build:
    - toolchain/gcc
