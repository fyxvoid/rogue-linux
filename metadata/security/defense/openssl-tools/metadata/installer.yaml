steps:
  - |
      echo "[COGMAN] Building openssl-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openssl-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
