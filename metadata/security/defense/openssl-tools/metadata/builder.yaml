steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting openssl-tools source, sir."
      tar -xf tar/openssl-tools-3.2.1.tar.xz -C source

  - |
      echo "[COGMAN] Building openssl-tools, sir."
      cd source/openssl-tools-3.2.1
      make -j$(nproc)

