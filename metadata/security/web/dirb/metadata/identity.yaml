name: dirb
version: "2.22"
category: security/web

summary: >
  Web content scanner using dictionary-based URL brute forcing

source:
  kind: tarball
  file: dirb-2.22.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/general/curl
