steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ssrfmap source, sir."
      tar -xf tar/ssrfmap-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building ssrfmap Python package, sir."
      cd source/ssrfmap-1.0
      python3 setup.py build

