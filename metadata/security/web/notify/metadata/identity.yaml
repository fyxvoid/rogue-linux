name: notify
version: "1.0.5"
category: security/web

summary: >
  Stream results to multiple notification platforms

source:
  kind: tarball
  file: notify-1.0.5.tar.xz

depends:
  build:
    - scripting/go/go
