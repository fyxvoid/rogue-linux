steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting notify source, sir."
      tar -xf tar/notify-1.0.5.tar.xz -C source

  - |
      echo "[COGMAN] Building notify with Go, sir."
      cd source/notify-1.0.5
      go build -o ../../build/ ./...

