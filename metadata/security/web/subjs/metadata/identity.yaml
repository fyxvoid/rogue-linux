name: subjs
version: "1.0.1"
category: security/web

summary: >
  Fetches JavaScript files from a list of URLs

source:
  kind: tarball
  file: subjs-1.0.1.tar.xz

depends:
  build:
    - scripting/go/go
