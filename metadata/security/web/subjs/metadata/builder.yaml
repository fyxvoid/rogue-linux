steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting subjs source, sir."
      tar -xf tar/subjs-1.0.1.tar.xz -C source

  - |
      echo "[COGMAN] Building subjs with Go, sir."
      cd source/subjs-1.0.1
      go build -o ../../build/ ./...

