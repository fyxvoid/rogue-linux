steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ffuf source, sir."
      tar -xf tar/ffuf-2.1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building ffuf with Go, sir."
      cd source/ffuf-2.1.0
      go build -o ../../build/ ./...

