steps:
  - |
      echo "[COGMAN] Building davtest, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing davtest into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
