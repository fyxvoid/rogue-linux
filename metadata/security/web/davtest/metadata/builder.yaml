steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting davtest source, sir."
      tar -xf tar/davtest-1.2.tar.xz -C source

  - |
      echo "[COGMAN] Building davtest, sir."
      cd source/davtest-1.2
      make -j$(nproc)

