steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting tplmap source, sir."
      tar -xf tar/tplmap-0.5.tar.xz -C source

  - |
      echo "[COGMAN] Building tplmap Python package, sir."
      cd source/tplmap-0.5
      python3 setup.py build

