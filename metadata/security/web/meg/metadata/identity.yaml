name: meg
version: "0.3.0"
category: security/web

summary: >
  Fetch many paths for many hosts without flooding servers

source:
  kind: tarball
  file: meg-0.3.0.tar.xz

depends:
  build:
    - scripting/go/go
