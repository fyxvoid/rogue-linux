steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting sqlmap source, sir."
      tar -xf tar/sqlmap-1.8.2.tar.xz -C source

  - |
      echo "[COGMAN] Building sqlmap Python package, sir."
      cd source/sqlmap-1.8.2
      python3 setup.py build

