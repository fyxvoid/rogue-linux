steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting nosqlmap source, sir."
      tar -xf tar/nosqlmap-0.7.tar.xz -C source

  - |
      echo "[COGMAN] Building nosqlmap Python package, sir."
      cd source/nosqlmap-0.7
      python3 setup.py build

