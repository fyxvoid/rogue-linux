steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting webshells source, sir."
      tar -xf tar/webshells-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building webshells, sir."
      cd source/webshells-1.0
      make -j$(nproc)

