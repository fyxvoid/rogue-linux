steps:
  - |
      echo "[COGMAN] Building webshells, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing webshells into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
