steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting cadaver source, sir."
      tar -xf tar/cadaver-0.24.tar.xz -C source

  - |
      echo "[COGMAN] Configuring cadaver, sir."
      cd build
      ../source/cadaver-0.24/configure --prefix=/usr \
        

