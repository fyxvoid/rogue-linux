name: cadaver
version: "0.24"
category: security/web

summary: >
  Command-line WebDAV client for testing WebDAV servers

source:
  kind: tarball
  file: cadaver-0.24.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
