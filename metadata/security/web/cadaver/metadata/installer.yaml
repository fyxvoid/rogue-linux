steps:
  - |
      echo "[COGMAN] Building cadaver, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cadaver into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
