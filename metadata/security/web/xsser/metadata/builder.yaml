steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting xsser source, sir."
      tar -xf tar/xsser-1.8.5.tar.xz -C source

  - |
      echo "[COGMAN] Building xsser Python package, sir."
      cd source/xsser-1.8.5
      python3 setup.py build

