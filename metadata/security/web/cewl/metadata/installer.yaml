steps:
  - |
      echo "[COGMAN] Building cewl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cewl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
