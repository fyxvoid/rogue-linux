steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting smuggler source, sir."
      tar -xf tar/smuggler-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building smuggler Python package, sir."
      cd source/smuggler-1.0
      python3 setup.py build

