name: smuggler
version: "1.0"
category: security/web

summary: >
  HTTP request smuggling and desync detection tool

source:
  kind: tarball
  file: smuggler-1.0.tar.xz

depends:
  build:
    - scripting/python/python3
