steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting jwt-tool source, sir."
      tar -xf tar/jwt-tool-2.2.7.tar.xz -C source

  - |
      echo "[COGMAN] Building jwt-tool Python package, sir."
      cd source/jwt-tool-2.2.7
      python3 setup.py build

