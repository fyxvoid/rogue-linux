name: gobuster
version: "3.6.0"
category: security/web

summary: >
  URI and DNS subdomain brute-force discovery tool

source:
  kind: tarball
  file: gobuster-3.6.0.tar.xz

depends:
  build:
    - scripting/go/go
