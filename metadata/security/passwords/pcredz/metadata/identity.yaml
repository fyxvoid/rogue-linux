name: pcredz
version: "2.0"
category: security/passwords

summary: >
  Extract credentials from pcap files and network interfaces

source:
  kind: tarball
  file: pcredz-2.0.tar.xz

depends:
  build:
    - scripting/python/python3
    - networking/core/libpcap
