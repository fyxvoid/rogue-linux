steps:
  - |
      echo "[COGMAN] Building hcxtools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hcxtools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
