steps:
  - |
      echo "[COGMAN] Building medusa, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing medusa into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
