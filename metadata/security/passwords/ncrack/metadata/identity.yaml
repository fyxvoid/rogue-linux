name: ncrack
version: "0.7"
category: security/passwords

summary: >
  High-speed network authentication cracking tool

source:
  kind: tarball
  file: ncrack-0.7.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - networking/core/libpcap
