steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ncrack source, sir."
      tar -xf tar/ncrack-0.7.tar.xz -C source

  - |
      echo "[COGMAN] Configuring ncrack, sir."
      cd build
      ../source/ncrack-0.7/configure --prefix=/usr \
        

