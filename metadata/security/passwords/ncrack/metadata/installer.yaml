steps:
  - |
      echo "[COGMAN] Building ncrack, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ncrack into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
