steps:
  - |
      echo "[COGMAN] Building cewl-pass, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cewl-pass into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
