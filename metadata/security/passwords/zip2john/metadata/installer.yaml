steps:
  - |
      echo "[COGMAN] Building zip2john, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing zip2john into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
