steps:
  - |
      echo "[COGMAN] Building cowpatty, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cowpatty into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
