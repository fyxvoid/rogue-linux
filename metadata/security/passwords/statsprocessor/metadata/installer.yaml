steps:
  - |
      echo "[COGMAN] Building statsprocessor, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing statsprocessor into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
