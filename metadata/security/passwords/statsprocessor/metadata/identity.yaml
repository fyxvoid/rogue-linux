name: statsprocessor
version: "0.14"
category: security/passwords

summary: >
  Word generator based on per-position Markov attack

source:
  kind: tarball
  file: statsprocessor-0.14.tar.xz

depends:
  build:
    - toolchain/gcc
