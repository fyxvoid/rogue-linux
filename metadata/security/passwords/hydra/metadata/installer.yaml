steps:
  - |
      echo "[COGMAN] Building hydra, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hydra into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
