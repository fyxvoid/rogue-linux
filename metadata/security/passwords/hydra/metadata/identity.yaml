name: hydra
version: "9.5"
category: security/passwords

summary: >
  Fast and flexible online password brute-forcing tool

source:
  kind: tarball
  file: hydra-9.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/crypto/libssh
    - networking/core/libpcap
