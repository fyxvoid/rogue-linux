steps:
  - |
      echo "[COGMAN] Building thc-pptp-bruter, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing thc-pptp-bruter into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
