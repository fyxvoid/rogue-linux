name: default-credentials
version: "1.0"
category: security/passwords

summary: >
  Database of default credentials for common devices and services

source:
  kind: tarball
  file: default-credentials-1.0.tar.xz

depends:
  build:
    - base/bash
