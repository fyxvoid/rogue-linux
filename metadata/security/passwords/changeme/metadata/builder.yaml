steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting changeme source, sir."
      tar -xf tar/changeme-1.2.3.tar.xz -C source

  - |
      echo "[COGMAN] Building changeme Python package, sir."
      cd source/changeme-1.2.3
      python3 setup.py build

