steps:
  - |
      echo "[COGMAN] Building hashcat, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hashcat into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
