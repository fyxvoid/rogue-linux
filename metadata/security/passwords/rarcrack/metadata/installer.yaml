steps:
  - |
      echo "[COGMAN] Building rarcrack, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rarcrack into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
