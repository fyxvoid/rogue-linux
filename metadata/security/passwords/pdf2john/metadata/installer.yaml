steps:
  - |
      echo "[COGMAN] Building pdf2john, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pdf2john into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
