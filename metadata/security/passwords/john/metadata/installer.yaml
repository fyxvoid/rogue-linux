steps:
  - |
      echo "[COGMAN] Building john, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing john into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
