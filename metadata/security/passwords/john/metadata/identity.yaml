name: john
version: "1.9.0"
category: security/passwords

summary: >
  John the Ripper password security auditing and recovery tool

source:
  kind: tarball
  file: john-1.9.0.tar.gz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
