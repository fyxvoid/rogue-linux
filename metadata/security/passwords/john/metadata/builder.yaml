steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting john source, sir."
      tar -xf tar/john-1.9.0.tar.gz -C source

  - |
      echo "[COGMAN] Configuring john, sir."
      cd build
      ../source/john-1.9.0/configure --prefix=/usr \
        

