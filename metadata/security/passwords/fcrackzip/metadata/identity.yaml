name: fcrackzip
version: "1.0"
category: security/passwords

summary: >
  Fast password cracker for encrypted ZIP archives

source:
  kind: tarball
  file: fcrackzip-1.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/compression/zlib
