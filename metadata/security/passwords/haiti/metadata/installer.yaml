steps:
  - |
      echo "[COGMAN] Building haiti, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing haiti into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
