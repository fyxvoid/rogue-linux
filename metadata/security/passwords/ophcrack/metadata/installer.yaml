steps:
  - |
      echo "[COGMAN] Building ophcrack, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ophcrack into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
