steps:
  - |
      echo "[COGMAN] Building maskprocessor, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing maskprocessor into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
