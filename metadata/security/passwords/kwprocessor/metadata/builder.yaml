steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting kwprocessor source, sir."
      tar -xf tar/kwprocessor-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building kwprocessor, sir."
      cd source/kwprocessor-1.0
      make -j$(nproc)

