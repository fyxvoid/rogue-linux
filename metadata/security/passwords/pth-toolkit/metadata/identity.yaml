name: pth-toolkit
version: "1.0"
category: security/passwords

summary: >
  Pass-the-Hash toolkit for Windows authentication attacks

source:
  kind: tarball
  file: pth-toolkit-1.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
