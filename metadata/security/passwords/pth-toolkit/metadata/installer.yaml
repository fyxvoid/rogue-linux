steps:
  - |
      echo "[COGMAN] Building pth-toolkit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pth-toolkit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
