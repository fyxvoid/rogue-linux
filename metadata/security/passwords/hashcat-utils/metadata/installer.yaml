steps:
  - |
      echo "[COGMAN] Building hashcat-utils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hashcat-utils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
