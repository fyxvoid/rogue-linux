steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting keepass2john source, sir."
      tar -xf tar/keepass2john-1.9.0.tar.xz -C source

  - |
      echo "[COGMAN] Building keepass2john, sir."
      cd source/keepass2john-1.9.0
      make -j$(nproc)

