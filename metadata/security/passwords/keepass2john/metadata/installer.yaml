steps:
  - |
      echo "[COGMAN] Building keepass2john, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing keepass2john into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
