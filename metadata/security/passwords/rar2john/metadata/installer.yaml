steps:
  - |
      echo "[COGMAN] Building rar2john, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rar2john into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
