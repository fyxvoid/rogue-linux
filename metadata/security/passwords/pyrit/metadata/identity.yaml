name: pyrit
version: "0.5.1"
category: security/passwords

summary: >
  WPA/WPA2-PSK passphrase cracking with GPU acceleration

source:
  kind: tarball
  file: pyrit-0.5.1.tar.xz

depends:
  build:
    - scripting/python/python3
    - networking/core/libpcap
    - libs/crypto/openssl
