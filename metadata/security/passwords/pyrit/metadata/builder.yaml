steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting pyrit source, sir."
      tar -xf tar/pyrit-0.5.1.tar.xz -C source

  - |
      echo "[COGMAN] Building pyrit Python package, sir."
      cd source/pyrit-0.5.1
      python3 setup.py build

