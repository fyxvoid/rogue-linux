steps:
  - |
      echo "[COGMAN] Building pdfcrack, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pdfcrack into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
