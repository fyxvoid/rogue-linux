steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting name-that-hash source, sir."
      tar -xf tar/name-that-hash-1.11.0.tar.xz -C source

  - |
      echo "[COGMAN] Building name-that-hash Python package, sir."
      cd source/name-that-hash-1.11.0
      python3 setup.py build

