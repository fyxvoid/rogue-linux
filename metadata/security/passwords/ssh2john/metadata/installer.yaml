steps:
  - |
      echo "[COGMAN] Building ssh2john, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ssh2john into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
