steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ssh2john source, sir."
      tar -xf tar/ssh2john-1.9.0.tar.xz -C source

  - |
      echo "[COGMAN] Building ssh2john, sir."
      cd source/ssh2john-1.9.0
      make -j$(nproc)

