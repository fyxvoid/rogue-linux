name: ssh2john
version: "1.9.0"
category: security/passwords

summary: >
  Convert SSH private keys to John the Ripper format

source:
  kind: tarball
  file: ssh2john-1.9.0.tar.xz

depends:
  build:
    - security/passwords/john
