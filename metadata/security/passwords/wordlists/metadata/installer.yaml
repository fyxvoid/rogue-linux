steps:
  - |
      echo "[COGMAN] Building wordlists, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wordlists into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
