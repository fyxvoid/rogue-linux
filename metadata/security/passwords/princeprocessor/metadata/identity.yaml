name: princeprocessor
version: "0.22"
category: security/passwords

summary: >
  Standalone PRINCE password candidate generator for hashcat

source:
  kind: tarball
  file: princeprocessor-0.22.tar.xz

depends:
  build:
    - toolchain/gcc
