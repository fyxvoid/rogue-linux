steps:
  - |
      echo "[COGMAN] Building princeprocessor, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing princeprocessor into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
