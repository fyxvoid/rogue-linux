steps:
  - |
      echo "[COGMAN] Building userrecon, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing userrecon into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
