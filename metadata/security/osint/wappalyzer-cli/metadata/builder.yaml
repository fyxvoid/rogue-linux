steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting wappalyzer-cli source, sir."
      tar -xf tar/wappalyzer-cli-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building wappalyzer-cli, sir."
      cd source/wappalyzer-cli-1.0
      make -j$(nproc)

