steps:
  - |
      echo "[COGMAN] Building wappalyzer-cli, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wappalyzer-cli into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
