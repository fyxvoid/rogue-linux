steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting gitdorker source, sir."
      tar -xf tar/gitdorker-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building gitdorker Python package, sir."
      cd source/gitdorker-1.0
      python3 setup.py build

