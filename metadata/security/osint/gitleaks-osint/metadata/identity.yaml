name: gitleaks-osint
version: "8.18.2"
category: security/osint

summary: >
  Scan git repos for hardcoded secrets and credentials

source:
  kind: tarball
  file: gitleaks-osint-8.18.2.tar.xz

depends:
  build:
    - scripting/go/go
