steps:
  - |
      echo "[COGMAN] Building exiftool-osint, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing exiftool-osint into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
