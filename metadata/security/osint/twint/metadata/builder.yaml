steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting twint source, sir."
      tar -xf tar/twint-2.1.22.tar.xz -C source

  - |
      echo "[COGMAN] Building twint Python package, sir."
      cd source/twint-2.1.22
      python3 setup.py build

