steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting crtsh-cli source, sir."
      tar -xf tar/crtsh-cli-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building crtsh-cli Python package, sir."
      cd source/crtsh-cli-1.0
      python3 setup.py build

