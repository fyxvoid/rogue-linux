name: passivedns
version: "0.1.0"
category: security/osint

summary: >
  Passive DNS collection tool for network monitoring

source:
  kind: tarball
  file: passivedns-0.1.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
