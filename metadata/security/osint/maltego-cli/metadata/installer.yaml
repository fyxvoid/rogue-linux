steps:
  - |
      echo "[COGMAN] Building maltego-cli, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing maltego-cli into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
