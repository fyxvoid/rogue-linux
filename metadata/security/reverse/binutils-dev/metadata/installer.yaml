steps:
  - |
      echo "[COGMAN] Building binutils-dev, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing binutils-dev into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
