name: pin-tools
version: "3.28"
category: security/reverse

summary: >
  Intel Pin dynamic binary instrumentation framework

source:
  kind: tarball
  file: pin-tools-3.28.tar.xz

depends:
  build:
    - toolchain/gcc
