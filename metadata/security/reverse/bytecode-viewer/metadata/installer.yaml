steps:
  - |
      echo "[COGMAN] Building bytecode-viewer, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing bytecode-viewer into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
