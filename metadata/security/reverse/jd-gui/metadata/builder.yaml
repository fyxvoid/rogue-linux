steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting jd-gui source, sir."
      tar -xf tar/jd-gui-1.6.6.tar.xz -C source

  - |
      echo "[COGMAN] Building jd-gui, sir."
      cd source/jd-gui-1.6.6
      make -j$(nproc)

