name: patchelf
version: "0.18.0"
category: security/reverse

summary: >
  Modify dynamic ELF executables rpath and interpreter

source:
  kind: tarball
  file: patchelf-0.18.0.tar.xz

depends:
  build:
    - toolchain/gcc
