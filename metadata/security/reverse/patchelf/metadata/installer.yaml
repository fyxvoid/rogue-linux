steps:
  - |
      echo "[COGMAN] Building patchelf, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing patchelf into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
