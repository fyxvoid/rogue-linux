steps:
  - |
      echo "[COGMAN] Building iaito, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing iaito into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
