steps:
  - |
      echo "[COGMAN] Building fernflower, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing fernflower into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
