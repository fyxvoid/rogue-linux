steps:
  - |
      echo "[COGMAN] Building upx, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing upx into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
