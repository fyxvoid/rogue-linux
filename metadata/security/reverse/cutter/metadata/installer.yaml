steps:
  - |
      echo "[COGMAN] Building cutter, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cutter into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
