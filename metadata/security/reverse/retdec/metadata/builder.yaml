steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting retdec source, sir."
      tar -xf tar/retdec-5.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring retdec with CMake, sir."
      cd build
      cmake ../source/retdec-5.0 -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release

