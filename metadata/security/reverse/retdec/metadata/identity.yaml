name: retdec
version: "5.0"
category: security/reverse

summary: >
  Retargetable machine-code decompiler based on LLVM

source:
  kind: tarball
  file: retdec-5.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
