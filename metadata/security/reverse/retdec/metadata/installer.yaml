steps:
  - |
      echo "[COGMAN] Building retdec, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing retdec into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
