steps:
  - |
      echo "[COGMAN] Building ltrace-re, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ltrace-re into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
