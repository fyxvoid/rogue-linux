name: ltrace-re
version: "0.7.3"
category: security/reverse

summary: >
  Library call tracer for shared library debugging

source:
  kind: tarball
  file: ltrace-re-0.7.3.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/libelf
