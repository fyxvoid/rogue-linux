steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ltrace-re source, sir."
      tar -xf tar/ltrace-re-0.7.3.tar.xz -C source

  - |
      echo "[COGMAN] Configuring ltrace-re, sir."
      cd build
      ../source/ltrace-re-0.7.3/configure --prefix=/usr \
        

