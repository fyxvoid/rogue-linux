steps:
  - |
      echo "[COGMAN] Building ilspy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ilspy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
