steps:
  - |
      echo "[COGMAN] Building cfr, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing cfr into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
