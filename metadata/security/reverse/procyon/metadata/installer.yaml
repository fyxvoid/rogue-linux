steps:
  - |
      echo "[COGMAN] Building procyon, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing procyon into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
