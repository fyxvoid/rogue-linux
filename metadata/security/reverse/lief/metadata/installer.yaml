steps:
  - |
      echo "[COGMAN] Building lief, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing lief into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
