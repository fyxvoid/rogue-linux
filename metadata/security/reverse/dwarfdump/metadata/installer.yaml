steps:
  - |
      echo "[COGMAN] Building dwarfdump, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dwarfdump into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
