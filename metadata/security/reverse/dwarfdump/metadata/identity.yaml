name: dwarfdump
version: "0.9.2"
category: security/reverse

summary: >
  DWARF debugging information dump utility

source:
  kind: tarball
  file: dwarfdump-0.9.2.tar.xz

depends:
  build:
    - toolchain/gcc
