steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting r2pipe source, sir."
      tar -xf tar/r2pipe-1.8.5.tar.xz -C source

  - |
      echo "[COGMAN] Building r2pipe Python package, sir."
      cd source/r2pipe-1.8.5
      python3 setup.py build

