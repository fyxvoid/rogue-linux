steps:
  - |
      echo "[COGMAN] Building jadx, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing jadx into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
