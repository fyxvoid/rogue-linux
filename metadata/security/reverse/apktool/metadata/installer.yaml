steps:
  - |
      echo "[COGMAN] Building apktool, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing apktool into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
