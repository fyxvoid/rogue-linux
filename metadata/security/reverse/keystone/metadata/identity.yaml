name: keystone
version: "0.9.2"
category: security/reverse

summary: >
  Multi-architecture assembler framework

source:
  kind: tarball
  file: keystone-0.9.2.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
