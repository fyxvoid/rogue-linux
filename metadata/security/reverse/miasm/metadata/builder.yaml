steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting miasm source, sir."
      tar -xf tar/miasm-0.1.5.tar.xz -C source

  - |
      echo "[COGMAN] Building miasm Python package, sir."
      cd source/miasm-0.1.5
      python3 setup.py build

