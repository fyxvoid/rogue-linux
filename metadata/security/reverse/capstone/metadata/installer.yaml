steps:
  - |
      echo "[COGMAN] Building capstone, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing capstone into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
