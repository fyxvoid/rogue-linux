steps:
  - |
      echo "[COGMAN] Building dnspy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dnspy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
