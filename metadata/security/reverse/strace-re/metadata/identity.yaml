name: strace-re
version: "6.7"
category: security/reverse

summary: >
  System call tracer for binary behavior analysis

source:
  kind: tarball
  file: strace-re-6.7.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/libelf
