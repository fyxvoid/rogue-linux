steps:
  - |
      echo "[COGMAN] Building strace-re, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing strace-re into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
