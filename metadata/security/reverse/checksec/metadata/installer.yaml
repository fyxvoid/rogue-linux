steps:
  - |
      echo "[COGMAN] Building checksec, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing checksec into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
