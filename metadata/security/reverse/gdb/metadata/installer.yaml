steps:
  - |
      echo "[COGMAN] Building gdb, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gdb into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
