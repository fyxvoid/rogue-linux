steps:
  - |
      echo "[COGMAN] Building ghidra-cli, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ghidra-cli into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
