steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ghidra-cli source, sir."
      tar -xf tar/ghidra-cli-11.0.1.tar.xz -C source

  - |
      echo "[COGMAN] Building ghidra-cli, sir."
      cd source/ghidra-cli-11.0.1
      make -j$(nproc)

