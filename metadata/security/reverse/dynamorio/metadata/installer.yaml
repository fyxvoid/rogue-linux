steps:
  - |
      echo "[COGMAN] Building dynamorio, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dynamorio into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
