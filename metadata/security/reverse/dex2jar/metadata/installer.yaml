steps:
  - |
      echo "[COGMAN] Building dex2jar, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dex2jar into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
