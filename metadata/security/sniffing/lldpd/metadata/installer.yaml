steps:
  - |
      echo "[COGMAN] Building lldpd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing lldpd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
