name: proxychains
version: "4.17"
category: security/sniffing

summary: >
  TCP and DNS traffic redirector through proxy servers

source:
  kind: tarball
  file: proxychains-4.17.tar.xz

depends:
  build:
    - toolchain/gcc
