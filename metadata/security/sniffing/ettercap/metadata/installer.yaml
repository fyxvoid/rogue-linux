steps:
  - |
      echo "[COGMAN] Building ettercap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ettercap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
