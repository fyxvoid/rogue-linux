steps:
  - |
      echo "[COGMAN] Building darkstat, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing darkstat into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
