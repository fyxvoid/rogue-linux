steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ngrep source, sir."
      tar -xf tar/ngrep-1.47.tar.xz -C source

  - |
      echo "[COGMAN] Configuring ngrep, sir."
      cd build
      ../source/ngrep-1.47/configure --prefix=/usr \
        

