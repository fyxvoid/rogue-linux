steps:
  - |
      echo "[COGMAN] Building ngrep, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ngrep into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
