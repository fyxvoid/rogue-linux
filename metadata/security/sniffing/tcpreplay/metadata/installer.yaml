steps:
  - |
      echo "[COGMAN] Building tcpreplay, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcpreplay into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
