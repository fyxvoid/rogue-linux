steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting net-creds source, sir."
      tar -xf tar/net-creds-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building net-creds Python package, sir."
      cd source/net-creds-1.0
      python3 setup.py build

