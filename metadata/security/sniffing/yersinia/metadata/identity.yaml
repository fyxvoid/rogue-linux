name: yersinia
version: "0.8.2"
category: security/sniffing

summary: >
  Framework for layer 2 protocol attacks on STP CDP DTP

source:
  kind: tarball
  file: yersinia-0.8.2.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - base/ncurses
