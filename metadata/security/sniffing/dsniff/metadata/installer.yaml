steps:
  - |
      echo "[COGMAN] Building dsniff, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dsniff into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
