name: dsniff
version: "2.4b1"
category: security/sniffing

summary: >
  Collection of tools for network auditing and penetration testing

source:
  kind: tarball
  file: dsniff-2.4b1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - libs/crypto/openssl
