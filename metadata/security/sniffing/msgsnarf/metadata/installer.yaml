steps:
  - |
      echo "[COGMAN] Building msgsnarf, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing msgsnarf into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
