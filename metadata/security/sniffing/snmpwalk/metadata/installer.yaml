steps:
  - |
      echo "[COGMAN] Building snmpwalk, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing snmpwalk into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
