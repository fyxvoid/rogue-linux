name: snmpwalk
version: "5.9.4"
category: security/sniffing

summary: >
  SNMP network management protocol query tool

source:
  kind: tarball
  file: snmpwalk-5.9.4.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
