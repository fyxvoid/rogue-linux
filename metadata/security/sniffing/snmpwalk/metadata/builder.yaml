steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting snmpwalk source, sir."
      tar -xf tar/snmpwalk-5.9.4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring snmpwalk, sir."
      cd build
      ../source/snmpwalk-5.9.4/configure --prefix=/usr \
        

