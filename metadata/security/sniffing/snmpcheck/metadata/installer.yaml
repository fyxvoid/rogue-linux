steps:
  - |
      echo "[COGMAN] Building snmpcheck, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing snmpcheck into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
