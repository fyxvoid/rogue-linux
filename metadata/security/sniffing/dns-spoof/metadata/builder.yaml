steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting dns-spoof source, sir."
      tar -xf tar/dns-spoof-1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building dns-spoof Python package, sir."
      cd source/dns-spoof-1.0
      python3 setup.py build

