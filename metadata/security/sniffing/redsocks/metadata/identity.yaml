name: redsocks
version: "0.5"
category: security/sniffing

summary: >
  Transparent TCP-to-proxy redirector using iptables

source:
  kind: tarball
  file: redsocks-0.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/general/libevent
