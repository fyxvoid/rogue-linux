steps:
  - |
      echo "[COGMAN] Building redsocks, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing redsocks into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
