name: arpspoof
version: "2.4b1"
category: security/sniffing

summary: >
  ARP spoofing tool for redirecting traffic on local network

source:
  kind: tarball
  file: arpspoof-2.4b1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - security/recon/libnet
