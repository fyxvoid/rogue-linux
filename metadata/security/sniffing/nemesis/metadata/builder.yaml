steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting nemesis source, sir."
      tar -xf tar/nemesis-1.8.tar.xz -C source

  - |
      echo "[COGMAN] Configuring nemesis, sir."
      cd build
      ../source/nemesis-1.8/configure --prefix=/usr \
        

