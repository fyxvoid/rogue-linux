steps:
  - |
      echo "[COGMAN] Building nemesis, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nemesis into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
