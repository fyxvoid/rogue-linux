name: nemesis
version: "1.8"
category: security/sniffing

summary: >
  Command-line network packet crafting and injection utility

source:
  kind: tarball
  file: nemesis-1.8.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - security/recon/libnet
