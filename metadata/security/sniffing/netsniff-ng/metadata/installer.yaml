steps:
  - |
      echo "[COGMAN] Building netsniff-ng, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing netsniff-ng into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
