steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ssldump source, sir."
      tar -xf tar/ssldump-1.8.tar.xz -C source

  - |
      echo "[COGMAN] Building ssldump, sir."
      cd source/ssldump-1.8
      make -j$(nproc)

