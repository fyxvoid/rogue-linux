name: ssldump
version: "1.8"
category: security/sniffing

summary: >
  SSL/TLS network protocol analyzer showing decrypted data

source:
  kind: tarball
  file: ssldump-1.8.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - networking/core/libpcap
