steps:
  - |
      echo "[COGMAN] Building ssldump, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ssldump into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
