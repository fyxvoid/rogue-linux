steps:
  - |
      echo "[COGMAN] Building mailsnarf, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mailsnarf into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
