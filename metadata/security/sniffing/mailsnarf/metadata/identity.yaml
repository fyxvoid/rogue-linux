name: mailsnarf
version: "2.4b1"
category: security/sniffing

summary: >
  Sniff SMTP and POP email messages from network traffic

source:
  kind: tarball
  file: mailsnarf-2.4b1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
