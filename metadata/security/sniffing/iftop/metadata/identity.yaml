name: iftop
version: "1.0pre4"
category: security/sniffing

summary: >
  Display bandwidth usage on an interface by host

source:
  kind: tarball
  file: iftop-1.0pre4.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - base/ncurses
