steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting iftop source, sir."
      tar -xf tar/iftop-1.0pre4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring iftop, sir."
      cd build
      ../source/iftop-1.0pre4/configure --prefix=/usr \
        

