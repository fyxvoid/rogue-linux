steps:
  - |
      echo "[COGMAN] Building iftop, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing iftop into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
