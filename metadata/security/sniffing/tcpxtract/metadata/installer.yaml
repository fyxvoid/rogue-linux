steps:
  - |
      echo "[COGMAN] Building tcpxtract, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcpxtract into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
