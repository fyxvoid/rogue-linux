steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting tcpxtract source, sir."
      tar -xf tar/tcpxtract-1.0.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring tcpxtract, sir."
      cd build
      ../source/tcpxtract-1.0.1/configure --prefix=/usr \
        

