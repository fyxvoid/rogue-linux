name: tcpxtract
version: "1.0.1"
category: security/sniffing

summary: >
  Extract files from network traffic based on file signatures

source:
  kind: tarball
  file: tcpxtract-1.0.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
