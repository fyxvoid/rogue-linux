name: nethogs
version: "0.8.7"
category: security/sniffing

summary: >
  NetHogs per-process network bandwidth monitor

source:
  kind: tarball
  file: nethogs-0.8.7.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - base/ncurses
