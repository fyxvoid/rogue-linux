name: tcpflow
version: "1.6.1"
category: security/sniffing

summary: >
  TCP flow recorder for reconstructing TCP stream data

source:
  kind: tarball
  file: tcpflow-1.6.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
    - libs/compression/zlib
