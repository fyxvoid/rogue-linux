name: urlsnarf
version: "2.4b1"
category: security/sniffing

summary: >
  Sniff HTTP requests and output in CLF format

source:
  kind: tarball
  file: urlsnarf-2.4b1.tar.xz

depends:
  build:
    - toolchain/gcc
    - networking/core/libpcap
