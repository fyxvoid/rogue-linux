steps:
  - |
      echo "[COGMAN] Building urlsnarf, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing urlsnarf into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
