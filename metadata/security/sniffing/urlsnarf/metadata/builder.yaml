steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting urlsnarf source, sir."
      tar -xf tar/urlsnarf-2.4b1.tar.xz -C source

  - |
      echo "[COGMAN] Building urlsnarf, sir."
      cd source/urlsnarf-2.4b1
      make -j$(nproc)

