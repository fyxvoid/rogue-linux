name: bandwhich
version: "0.22.2"
category: security/sniffing

summary: >
  Terminal bandwidth utilization tool by process

source:
  kind: tarball
  file: bandwhich-0.22.2.tar.xz

depends:
  build:
    - scripting/rust/rust
