steps:
  - |
      echo "[COGMAN] Building mausezahn, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mausezahn into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
