steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting tcpick source, sir."
      tar -xf tar/tcpick-0.2.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring tcpick, sir."
      cd build
      ../source/tcpick-0.2.1/configure --prefix=/usr \
        

