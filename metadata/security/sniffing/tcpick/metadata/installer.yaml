steps:
  - |
      echo "[COGMAN] Building tcpick, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcpick into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
