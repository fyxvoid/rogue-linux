steps:
  - |
      echo "[COGMAN] Building fragrouter, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing fragrouter into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
