steps:
  - |
      echo "[COGMAN] Building pcapfix, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pcapfix into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
