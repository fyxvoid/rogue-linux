steps:
  - |
      echo "[COGMAN] Building tsocks, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tsocks into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
