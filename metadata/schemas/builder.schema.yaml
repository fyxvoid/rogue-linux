$schema: http://json-schema.org/draft-07/schema#
type: object
required:
  - steps

properties:
  steps:
    type: array
    items:
      type: string
    description: >
      Ordered shell commands used to build the package.
      Executed verbatim by the build executor.

additionalProperties: false
