steps:
  - |
      echo "[COGMAN] Building rust, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rust into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
