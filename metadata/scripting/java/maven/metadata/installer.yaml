steps:
  - |
      echo "[COGMAN] Building maven, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing maven into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
