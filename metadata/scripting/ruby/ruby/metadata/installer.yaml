steps:
  - |
      echo "[COGMAN] Building ruby, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ruby into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
