steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bundler source, sir."
      tar -xf tar/bundler-2.5.6.tar.xz -C source

  - |
      echo "[COGMAN] Building bundler Python package, sir."
      cd source/bundler-2.5.6
      python3 setup.py build

