steps:
  - |
      echo "[COGMAN] Building lua, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing lua into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
