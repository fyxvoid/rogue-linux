steps:
  - |
      echo "[COGMAN] Building luarocks, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing luarocks into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
