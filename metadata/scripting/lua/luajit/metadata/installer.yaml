steps:
  - |
      echo "[COGMAN] Building luajit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing luajit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
