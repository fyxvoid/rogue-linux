steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting scapy source, sir."
      tar -xf tar/scapy-2.5.0.tar.xz -C source

  - |
      echo "[COGMAN] Building scapy Python package, sir."
      cd source/scapy-2.5.0
      python3 setup.py build

