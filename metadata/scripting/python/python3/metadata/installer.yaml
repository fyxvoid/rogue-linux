steps:
  - |
      echo "[COGMAN] Building python3, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing python3 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
