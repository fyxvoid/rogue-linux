steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting pyyaml source, sir."
      tar -xf tar/pyyaml-6.0.1.tar.xz -C source

  - |
      echo "[COGMAN] Building pyyaml Python package, sir."
      cd source/pyyaml-6.0.1
      python3 setup.py build

