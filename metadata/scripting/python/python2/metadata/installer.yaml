steps:
  - |
      echo "[COGMAN] Building python2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing python2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
