steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting jinja2 source, sir."
      tar -xf tar/jinja2-3.1.3.tar.xz -C source

  - |
      echo "[COGMAN] Building jinja2 Python package, sir."
      cd source/jinja2-3.1.3
      python3 setup.py build

