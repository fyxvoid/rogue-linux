steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting urllib3 source, sir."
      tar -xf tar/urllib3-2.2.0.tar.xz -C source

  - |
      echo "[COGMAN] Building urllib3 Python package, sir."
      cd source/urllib3-2.2.0
      python3 setup.py build

