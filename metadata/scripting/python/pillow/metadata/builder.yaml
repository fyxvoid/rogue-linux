steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting pillow source, sir."
      tar -xf tar/pillow-10.2.0.tar.xz -C source

  - |
      echo "[COGMAN] Building pillow Python package, sir."
      cd source/pillow-10.2.0
      python3 setup.py build

