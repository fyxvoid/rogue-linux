steps:
  - |
      echo "[COGMAN] Building go, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing go into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
