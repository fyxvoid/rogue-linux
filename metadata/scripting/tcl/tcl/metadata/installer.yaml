steps:
  - |
      echo "[COGMAN] Building tcl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
