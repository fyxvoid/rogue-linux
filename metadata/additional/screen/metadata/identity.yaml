name: screen
version: "4.9.1"
category: additional

summary: >
  Terminal multiplexer with detach and multi-display support

source:
  kind: tarball
  file: screen-4.9.1.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/ncurses
