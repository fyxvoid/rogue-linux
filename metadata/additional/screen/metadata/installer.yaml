steps:
  - |
      echo "[COGMAN] Building screen, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing screen into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
