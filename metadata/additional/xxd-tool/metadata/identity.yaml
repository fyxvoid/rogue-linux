name: xxd-tool
version: "9.1"
category: additional

summary: >
  Make a hexdump or do the reverse from binary to hex

source:
  kind: tarball
  file: xxd-tool-9.1.tar.xz

depends:
  build:
    - toolchain/gcc
