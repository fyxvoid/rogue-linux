steps:
  - |
      echo "[COGMAN] Building tcsh, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcsh into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
