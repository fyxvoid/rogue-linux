steps:
  - |
      echo "[COGMAN] Building p7zip, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing p7zip into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
