steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting exa source, sir."
      tar -xf tar/exa-0.10.1.tar.xz -C source

  - |
      echo "[COGMAN] Building exa with Cargo, sir."
      cd source/exa-0.10.1
      cargo build --release

