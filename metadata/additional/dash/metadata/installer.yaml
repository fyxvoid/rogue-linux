steps:
  - |
      echo "[COGMAN] Building dash, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dash into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
