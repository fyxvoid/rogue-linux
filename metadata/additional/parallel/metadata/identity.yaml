name: parallel
version: "20240222"
category: additional

summary: >
  Shell tool for executing jobs in parallel using resources

source:
  kind: tarball
  file: parallel-20240222.tar.xz

depends:
  build:
    - toolchain/perl
