steps:
  - |
      echo "[COGMAN] Building toilet, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing toilet into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
