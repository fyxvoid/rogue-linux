steps:
  - |
      echo "[COGMAN] Building figlet, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing figlet into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
