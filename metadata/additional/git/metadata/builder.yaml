steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting git source, sir."
      tar -xf tar/git-2.44.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring git, sir."
      cd build
      ../source/git-2.44.0/configure --prefix=/usr \
        

