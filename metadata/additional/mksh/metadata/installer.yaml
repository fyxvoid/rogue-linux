steps:
  - |
      echo "[COGMAN] Building mksh, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mksh into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
