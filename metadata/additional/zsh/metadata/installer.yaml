steps:
  - |
      echo "[COGMAN] Building zsh, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing zsh into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
