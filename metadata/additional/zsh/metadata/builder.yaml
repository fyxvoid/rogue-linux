steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting zsh source, sir."
      tar -xf tar/zsh-5.9.tar.xz -C source

  - |
      echo "[COGMAN] Configuring zsh, sir."
      cd build
      ../source/zsh-5.9/configure --prefix=/usr \
        --enable-multibyte \
        --enable-pcre

