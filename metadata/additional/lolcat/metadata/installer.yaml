steps:
  - |
      echo "[COGMAN] Building lolcat, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing lolcat into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
