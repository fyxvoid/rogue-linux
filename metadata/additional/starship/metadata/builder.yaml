steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting starship source, sir."
      tar -xf tar/starship-1.17.1.tar.xz -C source

  - |
      echo "[COGMAN] Building starship with Cargo, sir."
      cd source/starship-1.17.1
      cargo build --release

