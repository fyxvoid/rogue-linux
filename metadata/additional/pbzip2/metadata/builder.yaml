steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting pbzip2 source, sir."
      tar -xf tar/pbzip2-1.1.13.tar.xz -C source

  - |
      echo "[COGMAN] Building pbzip2, sir."
      cd source/pbzip2-1.1.13
      make -j$(nproc)

