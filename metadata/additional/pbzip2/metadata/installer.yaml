steps:
  - |
      echo "[COGMAN] Building pbzip2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pbzip2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
