steps:
  - |
      echo "[COGMAN] Building tmux, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tmux into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
