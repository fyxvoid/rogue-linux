steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting tmux source, sir."
      tar -xf tar/tmux-3.4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring tmux, sir."
      cd build
      ../source/tmux-3.4/configure --prefix=/usr \
        

