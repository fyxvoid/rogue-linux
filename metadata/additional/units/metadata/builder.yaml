steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting units source, sir."
      tar -xf tar/units-2.23.tar.xz -C source

  - |
      echo "[COGMAN] Configuring units, sir."
      cd build
      ../source/units-2.23/configure --prefix=/usr \
        

