steps:
  - |
      echo "[COGMAN] Building tree, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tree into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
