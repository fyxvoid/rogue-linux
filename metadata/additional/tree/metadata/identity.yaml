name: tree
version: "2.1.1"
category: additional

summary: >
  List contents of directories in a tree-like format

source:
  kind: tarball
  file: tree-2.1.1.tar.xz

depends:
  build:
    - toolchain/gcc
