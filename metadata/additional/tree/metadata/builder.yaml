steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting tree source, sir."
      tar -xf tar/tree-2.1.1.tar.xz -C source

  - |
      echo "[COGMAN] Building tree, sir."
      cd source/tree-2.1.1
      make -j$(nproc)

