steps:
  - |
      echo "[COGMAN] Building hexedit, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hexedit into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
