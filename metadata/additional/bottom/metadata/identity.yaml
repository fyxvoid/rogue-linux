name: bottom
version: "0.9.6"
category: additional

summary: >
  Cross-platform graphical process and system monitor

source:
  kind: tarball
  file: bottom-0.9.6.tar.xz

depends:
  build:
    - scripting/rust/rust
