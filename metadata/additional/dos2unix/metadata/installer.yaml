steps:
  - |
      echo "[COGMAN] Building dos2unix, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dos2unix into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
