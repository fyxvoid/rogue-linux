steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting ripgrep source, sir."
      tar -xf tar/ripgrep-14.1.0.tar.xz -C source

  - |
      echo "[COGMAN] Building ripgrep with Cargo, sir."
      cd source/ripgrep-14.1.0
      cargo build --release

