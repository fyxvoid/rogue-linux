steps:
  - |
      echo "[COGMAN] Building zip, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing zip into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
