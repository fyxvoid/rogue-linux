name: pigz
version: "2.8"
category: additional

summary: >
  Parallel implementation of gzip for multi-core compression

source:
  kind: tarball
  file: pigz-2.8.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/compression/zlib
