steps:
  - |
      echo "[COGMAN] Building pv, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pv into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
