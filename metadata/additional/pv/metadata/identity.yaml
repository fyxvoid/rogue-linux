name: pv
version: "1.8.5"
category: additional

summary: >
  Pipe Viewer for monitoring progress of data through pipeline

source:
  kind: tarball
  file: pv-1.8.5.tar.xz

depends:
  build:
    - toolchain/gcc
