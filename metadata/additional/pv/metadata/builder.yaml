steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting pv source, sir."
      tar -xf tar/pv-1.8.5.tar.xz -C source

  - |
      echo "[COGMAN] Configuring pv, sir."
      cd build
      ../source/pv-1.8.5/configure --prefix=/usr \
        

