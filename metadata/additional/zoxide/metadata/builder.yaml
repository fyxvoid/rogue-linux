steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting zoxide source, sir."
      tar -xf tar/zoxide-0.9.4.tar.xz -C source

  - |
      echo "[COGMAN] Building zoxide with Cargo, sir."
      cd source/zoxide-0.9.4
      cargo build --release

