steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting time source, sir."
      tar -xf tar/time-1.9.tar.xz -C source

  - |
      echo "[COGMAN] Configuring time, sir."
      cd build
      ../source/time-1.9/configure --prefix=/usr \
        

