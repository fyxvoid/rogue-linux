name: zstd
version: "1.5.5"
category: libs/compression

summary: >
  Zstandard fast real-time compression algorithm

source:
  kind: tarball
  file: zstd-1.5.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
