steps:
  - |
      echo "[COGMAN] Building zstd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing zstd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
