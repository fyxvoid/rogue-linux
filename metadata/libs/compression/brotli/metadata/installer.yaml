steps:
  - |
      echo "[COGMAN] Building brotli, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing brotli into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
