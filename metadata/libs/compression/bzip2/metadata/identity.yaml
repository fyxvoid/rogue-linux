name: bzip2
version: "1.0.8"
category: libs/compression

summary: >
  Block-sorting file compressor library and utilities

source:
  kind: tarball
  file: bzip2-1.0.8.tar.gz

depends:
  build:
    - toolchain/gcc
