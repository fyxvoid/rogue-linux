steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bzip2 source, sir."
      tar -xf tar/bzip2-1.0.8.tar.gz -C source

  - |
      echo "[COGMAN] Building bzip2, sir."
      cd source/bzip2-1.0.8
      make -j$(nproc)

