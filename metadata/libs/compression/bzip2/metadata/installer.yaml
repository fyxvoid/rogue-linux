steps:
  - |
      echo "[COGMAN] Building bzip2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing bzip2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
