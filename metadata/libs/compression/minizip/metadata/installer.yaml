steps:
  - |
      echo "[COGMAN] Building minizip, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing minizip into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
