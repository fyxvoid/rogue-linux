steps:
  - |
      echo "[COGMAN] Building libarchive, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libarchive into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
