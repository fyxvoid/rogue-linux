name: snappy
version: "1.2.0"
category: libs/compression

summary: >
  Fast compression and decompression library by Google

source:
  kind: tarball
  file: snappy-1.2.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - toolchain/cmake
