steps:
  - |
      echo "[COGMAN] Building snappy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing snappy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
