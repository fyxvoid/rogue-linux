steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting xz source, sir."
      tar -xf tar/xz-5.4.6.tar.xz -C source

  - |
      echo "[COGMAN] Configuring xz, sir."
      cd build
      ../source/xz-5.4.6/configure --prefix=/usr \
        --disable-static

