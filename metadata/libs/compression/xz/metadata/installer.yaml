steps:
  - |
      echo "[COGMAN] Building xz, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing xz into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
