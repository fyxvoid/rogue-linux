name: zlib
version: "1.3"
category: base

summary: >
  Compression library required by countless system and
  application packages.

source:
  kind: tarball
  file: zlib-1.3.1.tar.gz

depends:
  build:
    - toolchain/gcc
    - toolchain/glibc
