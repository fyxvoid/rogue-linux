name: zlib
version: "1.3.1"
category: libs/compression

summary: >
  General purpose lossless data compression library

source:
  kind: tarball
  file: zlib-1.3.1.tar.xz

depends:
  build:
    - toolchain/gcc
