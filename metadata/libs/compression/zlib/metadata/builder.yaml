steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting zlib source, sir."
      tar -xf tar/zlib-1.3.1.tar.gz -C source

  - |
      echo "[COGMAN] Configuring zlib, sir."
      cd source/zlib-1.3.1

      ./configure --prefix=/usr
