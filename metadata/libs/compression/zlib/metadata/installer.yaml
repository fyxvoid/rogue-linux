steps:
  - |
      echo "[COGMAN] Building zlib, sir."
      cd source/zlib-1.3.1
      make -j1

  - |
      echo "[COGMAN] Installing zlib into staging root, sir."
      cd source/zlib-1.3.1
      make install DESTDIR="$PKGROOT"
