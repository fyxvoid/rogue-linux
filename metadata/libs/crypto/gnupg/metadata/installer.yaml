steps:
  - |
      echo "[COGMAN] Building gnupg, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gnupg into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
