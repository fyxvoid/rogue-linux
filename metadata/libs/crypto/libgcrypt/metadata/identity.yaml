name: libgcrypt
version: "1.10.3"
category: libs/crypto

summary: >
  GNU cryptographic library providing symmetric and asymmetric ciphers

source:
  kind: tarball
  file: libgcrypt-1.10.3.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/libgpg-error
