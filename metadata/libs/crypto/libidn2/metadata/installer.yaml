steps:
  - |
      echo "[COGMAN] Building libidn2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libidn2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
