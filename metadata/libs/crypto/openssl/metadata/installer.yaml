steps:
  - |
      echo "[COGMAN] Building openssl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openssl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
