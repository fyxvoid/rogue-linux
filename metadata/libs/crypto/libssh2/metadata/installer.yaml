steps:
  - |
      echo "[COGMAN] Building libssh2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libssh2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
