steps:
  - |
      echo "[COGMAN] Building nss, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nss into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
