steps:
  - |
      echo "[COGMAN] Building wolfssl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wolfssl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
