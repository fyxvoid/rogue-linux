steps:
  - |
      echo "[COGMAN] Building libunistring, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libunistring into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
