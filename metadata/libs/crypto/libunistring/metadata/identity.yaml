name: libunistring
version: "1.2"
category: libs/crypto

summary: >
  Unicode string library for C providing Unicode operations

source:
  kind: tarball
  file: libunistring-1.2.tar.xz

depends:
  build:
    - toolchain/gcc
