steps:
  - |
      echo "[COGMAN] Building libgpg-error, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libgpg-error into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
