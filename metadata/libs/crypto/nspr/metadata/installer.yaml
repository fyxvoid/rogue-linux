steps:
  - |
      echo "[COGMAN] Building nspr, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nspr into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
