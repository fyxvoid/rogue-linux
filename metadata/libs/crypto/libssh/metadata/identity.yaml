name: libssh
version: "0.10.6"
category: libs/crypto

summary: >
  SSH client and server library implementing SSHv2 protocol

source:
  kind: tarball
  file: libssh-0.10.6.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/compression/zlib
