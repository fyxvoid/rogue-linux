steps:
  - |
      echo "[COGMAN] Building libssh, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libssh into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
