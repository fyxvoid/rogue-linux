steps:
  - |
      echo "[COGMAN] Building gnutls, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gnutls into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
