steps:
  - |
      echo "[COGMAN] Building ca-certificates, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ca-certificates into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
