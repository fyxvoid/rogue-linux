steps:
  - |
      echo "[COGMAN] Building gpgme, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing gpgme into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
