steps:
  - |
      echo "[COGMAN] Building mbedtls, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing mbedtls into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
