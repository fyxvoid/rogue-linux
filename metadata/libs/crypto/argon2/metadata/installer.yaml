steps:
  - |
      echo "[COGMAN] Building argon2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing argon2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
