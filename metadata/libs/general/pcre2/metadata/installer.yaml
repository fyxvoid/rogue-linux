steps:
  - |
      echo "[COGMAN] Building pcre2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pcre2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
