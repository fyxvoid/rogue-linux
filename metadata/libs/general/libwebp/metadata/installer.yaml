steps:
  - |
      echo "[COGMAN] Building libwebp, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libwebp into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
