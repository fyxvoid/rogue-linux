name: curl
version: "8.6.0"
category: libs/general

summary: >
  Command line tool and library for transferring data with URLs

source:
  kind: tarball
  file: curl-8.6.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/compression/zlib
    - libs/crypto/libssh2
    - libs/general/nghttp2
