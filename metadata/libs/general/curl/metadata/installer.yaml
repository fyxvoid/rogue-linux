steps:
  - |
      echo "[COGMAN] Building curl, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing curl into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
