steps:
  - |
      echo "[COGMAN] Building libjpeg-turbo, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libjpeg-turbo into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
