name: wget
version: "1.24.5"
category: libs/general

summary: >
  Non-interactive network file downloader supporting HTTP HTTPS and FTP

source:
  kind: tarball
  file: wget-1.24.5.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/compression/zlib
