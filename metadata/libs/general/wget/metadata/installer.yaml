steps:
  - |
      echo "[COGMAN] Building wget, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wget into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
