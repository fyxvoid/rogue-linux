steps:
  - |
      echo "[COGMAN] Building protobuf, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing protobuf into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
