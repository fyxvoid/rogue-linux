steps:
  - |
      echo "[COGMAN] Building giflib, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing giflib into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
