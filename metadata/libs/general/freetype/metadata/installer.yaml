steps:
  - |
      echo "[COGMAN] Building freetype, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing freetype into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
