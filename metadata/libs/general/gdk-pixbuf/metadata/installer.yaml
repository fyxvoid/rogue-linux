steps:
  - |
      echo "[COGMAN] Building gdk-pixbuf, sir."
      cd build
      ninja

  - |
      echo "[COGMAN] Installing gdk-pixbuf into staging root, sir."
      cd build
      DESTDIR="$PKGROOT" ninja install
