steps:
  - |
      echo "[COGMAN] Building libffi, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libffi into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
