steps:
  - |
      echo "[COGMAN] Building libzip, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libzip into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
