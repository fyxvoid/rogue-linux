steps:
  - |
      echo "[COGMAN] Building jq, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing jq into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
