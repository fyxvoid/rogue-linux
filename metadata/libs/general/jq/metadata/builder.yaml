steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting jq source, sir."
      tar -xf tar/jq-1.7.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring jq, sir."
      cd build
      ../source/jq-1.7.1/configure --prefix=/usr \
        --disable-static

