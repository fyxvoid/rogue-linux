steps:
  - |
      echo "[COGMAN] Building oniguruma, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing oniguruma into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
