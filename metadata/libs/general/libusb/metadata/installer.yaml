steps:
  - |
      echo "[COGMAN] Building libusb, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libusb into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
