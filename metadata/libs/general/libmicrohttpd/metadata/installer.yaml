steps:
  - |
      echo "[COGMAN] Building libmicrohttpd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libmicrohttpd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
