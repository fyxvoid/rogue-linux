name: libmicrohttpd
version: "1.0.0"
category: libs/general

summary: >
  Small C library for running HTTP server as part of an application

source:
  kind: tarball
  file: libmicrohttpd-1.0.0.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/gnutls
