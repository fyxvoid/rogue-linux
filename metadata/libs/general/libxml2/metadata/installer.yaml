steps:
  - |
      echo "[COGMAN] Building libxml2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libxml2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
