name: popt
version: "1.19"
category: libs/general

summary: >
  Command line option parsing library with table-driven approach

source:
  kind: tarball
  file: popt-1.19.tar.xz

depends:
  build:
    - toolchain/gcc
