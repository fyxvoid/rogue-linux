steps:
  - |
      echo "[COGMAN] Building popt, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing popt into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
