steps:
  - |
      echo "[COGMAN] Building pcre, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing pcre into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
