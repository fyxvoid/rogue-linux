steps:
  - |
      echo "[COGMAN] Building libxslt, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libxslt into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
