steps:
  - |
      echo "[COGMAN] Building c-ares, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing c-ares into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
