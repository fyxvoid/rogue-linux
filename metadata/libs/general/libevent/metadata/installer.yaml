steps:
  - |
      echo "[COGMAN] Building libevent, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libevent into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
