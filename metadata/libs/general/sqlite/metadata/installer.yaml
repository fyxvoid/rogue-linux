steps:
  - |
      echo "[COGMAN] Building sqlite, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing sqlite into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
