steps:
  - |
      echo "[COGMAN] Building libtiff, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libtiff into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
