steps:
  - |
      echo "[COGMAN] Building msgpack, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing msgpack into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
