steps:
  - |
      echo "[COGMAN] Building libuv, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libuv into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
