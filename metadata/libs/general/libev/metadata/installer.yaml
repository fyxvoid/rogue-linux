steps:
  - |
      echo "[COGMAN] Building libev, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libev into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
