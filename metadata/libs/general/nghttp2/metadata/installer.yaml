steps:
  - |
      echo "[COGMAN] Building nghttp2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nghttp2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
