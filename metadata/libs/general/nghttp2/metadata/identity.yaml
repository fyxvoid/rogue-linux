name: nghttp2
version: "1.59.0"
category: libs/general

summary: >
  HTTP/2 C library and tools for multiplexed connections

source:
  kind: tarball
  file: nghttp2-1.59.0.tar.xz

depends:
  build:
    - toolchain/gcc
