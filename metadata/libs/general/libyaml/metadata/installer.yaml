steps:
  - |
      echo "[COGMAN] Building libyaml, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libyaml into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
