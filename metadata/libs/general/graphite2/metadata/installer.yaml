steps:
  - |
      echo "[COGMAN] Building graphite2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing graphite2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
