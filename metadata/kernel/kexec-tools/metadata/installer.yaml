steps:
  - |
      echo "[COGMAN] Building kexec-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing kexec-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
