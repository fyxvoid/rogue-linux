steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting kexec-tools source, sir."
      tar -xf tar/kexec-tools-2.0.28.tar.xz -C source

  - |
      echo "[COGMAN] Configuring kexec-tools, sir."
      cd build
      ../source/kexec-tools-2.0.28/configure --prefix=/usr \
        

