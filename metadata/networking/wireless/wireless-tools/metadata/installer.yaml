steps:
  - |
      echo "[COGMAN] Building wireless-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wireless-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
