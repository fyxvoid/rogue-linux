steps:
  - |
      echo "[COGMAN] Building iw, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing iw into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
