steps:
  - |
      echo "[COGMAN] Building bluez, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing bluez into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
