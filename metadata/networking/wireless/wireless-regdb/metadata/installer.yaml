steps:
  - |
      echo "[COGMAN] Building wireless-regdb, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wireless-regdb into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
