steps:
  - |
      echo "[COGMAN] Building rfkill, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rfkill into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
