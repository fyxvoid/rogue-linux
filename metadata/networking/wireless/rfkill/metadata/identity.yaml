name: rfkill
version: "1.0"
category: networking/wireless

summary: >
  Tool for enabling and disabling wireless devices

source:
  kind: tarball
  file: rfkill-1.0.tar.xz

depends:
  build:
    - toolchain/gcc
