steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bluez-tools source, sir."
      tar -xf tar/bluez-tools-2.0.tar.xz -C source

  - |
      echo "[COGMAN] Configuring bluez-tools, sir."
      cd build
      ../source/bluez-tools-2.0/configure --prefix=/usr \
        

