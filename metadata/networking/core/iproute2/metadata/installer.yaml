steps:
  - |
      echo "[COGMAN] Building iproute2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing iproute2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
