steps:
  - |
      echo "[COGMAN] Building dhcpcd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dhcpcd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
