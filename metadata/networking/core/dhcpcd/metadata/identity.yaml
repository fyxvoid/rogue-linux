name: dhcpcd
version: "10.0.6"
category: networking/core

summary: >
  DHCP client daemon for automatic network configuration

source:
  kind: tarball
  file: dhcpcd-10.0.6.tar.xz

depends:
  build:
    - toolchain/gcc
    - system/eudev
