steps:
  - |
      echo "[COGMAN] Building hostapd, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing hostapd into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
