steps:
  - |
      echo "[COGMAN] Building whois, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing whois into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
