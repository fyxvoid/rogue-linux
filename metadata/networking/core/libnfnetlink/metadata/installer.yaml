steps:
  - |
      echo "[COGMAN] Building libnfnetlink, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libnfnetlink into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
