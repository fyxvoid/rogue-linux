steps:
  - |
      echo "[COGMAN] Building nginx, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nginx into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
