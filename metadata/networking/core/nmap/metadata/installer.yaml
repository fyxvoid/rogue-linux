steps:
  - |
      echo "[COGMAN] Building nmap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing nmap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
