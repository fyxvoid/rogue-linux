steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting traceroute source, sir."
      tar -xf tar/traceroute-2.1.3.tar.xz -C source

  - |
      echo "[COGMAN] Building traceroute, sir."
      cd source/traceroute-2.1.3
      make -j$(nproc)

