name: traceroute
version: "2.1.3"
category: networking/core

summary: >
  Utility for tracing the route packets take to a network host

source:
  kind: tarball
  file: traceroute-2.1.3.tar.xz

depends:
  build:
    - toolchain/gcc
