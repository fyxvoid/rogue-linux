steps:
  - |
      echo "[COGMAN] Building traceroute, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing traceroute into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
