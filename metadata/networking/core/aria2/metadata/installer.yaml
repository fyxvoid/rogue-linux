steps:
  - |
      echo "[COGMAN] Building aria2, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing aria2 into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
