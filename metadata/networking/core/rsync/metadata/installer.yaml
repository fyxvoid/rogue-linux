steps:
  - |
      echo "[COGMAN] Building rsync, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing rsync into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
