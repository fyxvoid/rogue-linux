name: mtr
version: "0.95"
category: networking/core

summary: >
  Network diagnostic tool combining ping and traceroute

source:
  kind: tarball
  file: mtr-0.95.tar.xz

depends:
  build:
    - toolchain/gcc
    - base/ncurses
