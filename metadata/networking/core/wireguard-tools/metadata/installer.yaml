steps:
  - |
      echo "[COGMAN] Building wireguard-tools, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing wireguard-tools into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
