steps:
  - |
      echo "[COGMAN] Building tcpdump, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing tcpdump into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
