steps:
  - |
      echo "[COGMAN] Building iptables, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing iptables into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
