steps:
  - |
      echo "[COGMAN] Building openvpn, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing openvpn into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
