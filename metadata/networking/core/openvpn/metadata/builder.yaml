steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting openvpn source, sir."
      tar -xf tar/openvpn-2.6.9.tar.xz -C source

  - |
      echo "[COGMAN] Configuring openvpn, sir."
      cd build
      ../source/openvpn-2.6.9/configure --prefix=/usr \
        --enable-iproute2

