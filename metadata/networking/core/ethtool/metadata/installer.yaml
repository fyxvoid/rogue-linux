steps:
  - |
      echo "[COGMAN] Building ethtool, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ethtool into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
