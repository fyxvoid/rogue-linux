steps:
  - |
      echo "[COGMAN] Building chrony, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing chrony into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
