name: stunnel
version: "5.72"
category: networking/core

summary: >
  TLS/SSL tunneling wrapper for legacy applications

source:
  kind: tarball
  file: stunnel-5.72.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
