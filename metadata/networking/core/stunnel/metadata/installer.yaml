steps:
  - |
      echo "[COGMAN] Building stunnel, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing stunnel into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
