steps:
  - |
      echo "[COGMAN] Building haproxy, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing haproxy into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
