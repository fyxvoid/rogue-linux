steps:
  - |
      echo "[COGMAN] Building bridge-utils, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing bridge-utils into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
