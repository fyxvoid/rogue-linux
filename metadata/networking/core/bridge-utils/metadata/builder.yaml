steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting bridge-utils source, sir."
      tar -xf tar/bridge-utils-1.7.1.tar.xz -C source

  - |
      echo "[COGMAN] Configuring bridge-utils, sir."
      cd build
      ../source/bridge-utils-1.7.1/configure --prefix=/usr \
        

