steps:
  - |
      echo "[COGMAN] Building strongswan, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing strongswan into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
