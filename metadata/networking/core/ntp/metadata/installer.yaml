steps:
  - |
      echo "[COGMAN] Building ntp, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing ntp into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
