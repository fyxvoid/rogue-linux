steps:
  - |
      echo "[COGMAN] Building dnsmasq, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing dnsmasq into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
