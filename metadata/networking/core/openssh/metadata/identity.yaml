name: openssh
version: "9.6p1"
category: networking/core

summary: >
  OpenSSH secure shell client server and key management tools

source:
  kind: tarball
  file: openssh-9.6p1.tar.xz

depends:
  build:
    - toolchain/gcc
    - libs/crypto/openssl
    - libs/compression/zlib
