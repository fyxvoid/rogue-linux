steps:
  - |
      echo "[COGMAN] Building libpcap, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing libpcap into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
