steps:
  - rm -rf source build
  - mkdir -p source build

  - |
      echo "[COGMAN] Extracting libpcap source, sir."
      tar -xf tar/libpcap-1.10.4.tar.xz -C source

  - |
      echo "[COGMAN] Configuring libpcap, sir."
      cd build
      ../source/libpcap-1.10.4/configure --prefix=/usr \
        

