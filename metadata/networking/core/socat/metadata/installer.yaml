steps:
  - |
      echo "[COGMAN] Building socat, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing socat into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
