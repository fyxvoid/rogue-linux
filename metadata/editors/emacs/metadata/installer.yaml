steps:
  - |
      echo "[COGMAN] Building emacs, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing emacs into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
