steps:
  - |
      echo "[COGMAN] Building kakoune, sir."
      cd build
      make -j$(nproc)

  - |
      echo "[COGMAN] Installing kakoune into staging root, sir."
      cd build
      make install DESTDIR="$PKGROOT"
